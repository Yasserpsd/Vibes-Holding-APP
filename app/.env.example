# Copy to app/.env (git-ignored). Public value only: the server base URL without a trailing slash.
# Phone on the same Wi-Fi as this PC: http://<pc-ip>:3000  |  testers: the Railway test URL.
EXPO_PUBLIC_API_URL=
