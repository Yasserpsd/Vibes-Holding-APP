# Project brief — تطبيق نادي المستثمرين

Last updated: 2026-09-16 (owner's closing-scope decisions recorded in §5.5 and as M9 to M12: «رسائل الإدارة» first, neutral first with «دليل المحايد» and workshops, «شخصية ومسيرة» self-application; members' chat cancelled the same day; the dashboard gets its own domain; M7 part 2 B is open as PR #5 on branch `m7-payments`, waiting for the store accounts, Firebase and RevenueCat setup and the owner's OK for ONE preview build). These are the owner's decisions. If anything here conflicts with what the owner says in a session, ask before acting.

## 1. Entities and contacts
- Publisher and membership seller: شركة المجتمع الافتراضي للاستثمار, the official operator of نادي المستثمرين (vcmem.com). CR 2050179051, VAT 311933896300003, Riyadh, Al Olaya.
- Parent: فايبز القابضة (شركة ذبذبات للاستثمار القابضة), owner of the «نادي المستثمرين®» trademark. Founder: م. سالم المسرحي.
- Management (the only management number): +966 55 831 8777.
- Studio bookings (بودكاست الملتقى): +966 53 846 1110.
- Club size: 11,000+ members. Target 20,000, then 100,000 after the app launch.

## 2. Architecture
The app calls only `server/`. The server talks to:
- **vcmem.com hub** (WordPress, Vibes AI Assistant plugin in hub mode): accounts, e-mail OTP, memberships, AI chat (n8n workflow "Vibes Web Agent"), staff console, admin roles. REST namespace `vibes-ai/v1`.
- **vibesholding.com Projects Bank** (plugin `projects-directory-pro`): projects feed and unlocks.
- **Paymob KSA**: real-world services only, Intention API, per-payment notification URL pointing to the server.
- **App stores via RevenueCat** (later): membership purchase → server → hub activation.
- **News sources + OpenAI API** (later): classification, deduplication and ranking only.

### 2.1 Hub bridge (later milestone)
The hub needs server-to-server endpoints secured by a bridge key. They are added to the Vibes AI Assistant plugin in a Claude chat session where the owner uploads the current plugin zip. Never guess the plugin's code.
Needed: register with OTP, verify/resend OTP, login returning a token for the server, profile get/update/avatar, delete account, membership status (unactivated, or annual with days left, AI «رصيد», project unlocks left), activate membership from a store purchase, chat send (text/voice/image) and history, staff-reply webhook to the server (for push), admin check.

### 2.2 Projects Bank feed
- `GET https://vibesholding.com/wp-json/pb/v1/projects?key=…` (plus `/ping`). The key lives in server env only.
- Post type `project`. Meta: project_number, company_name, founder_name, whatsapp, email, website, pitch_deck, project_gallery, project_details (Arabic body; post_content is empty), project_details_en, excerpt_en, title_en, company_name_en, founder_name_en, is_featured (golden flag), featured_order, golden_partner_url, views_count. Taxonomies: `sector`, `project_stage`.
- whatsapp, email, website and pitch_deck are private (CLAUDE.md rule 4).
- Unlocks live in table `wp_pdp_unlocks`, keyed by the hub contact id. A paid member gets 5 project unlocks per membership year. Golden projects never deduct credit and route to golden_partner_url.
- A server-to-server unlock endpoint is added to the Projects Bank plugin in a later milestone (the owner uploads the plugin zip in a Claude chat).
- Sort options used on the web: الأحدث / الأكثر مشاهدة / اكتشف / المشاريع الذهبية.

## 3. Accounts (mirror the hub; the hub enforces them)
- E-mail: Gmail, iCloud, Outlook, or a Saudi `.sa` domain only, verified by e-mail OTP.
- Phone: Saudi and GCC numbers only, Saudi by default, format `0558318777` (no spaces, no country code); other GCC countries selectable. Notice text: «التسجيل متاح للسعوديين والمقيمين ودول مجلس التعاون».
- Mandatory persona: «رائد أعمال — لدي مشروع مميز» / «مستثمر — أبحث عن فرص شراكة واعدة» / «محايد — شريك المستقبل وسأحدد توجهي لاحقًا». Mandatory short bio. Optional job title, photo, social links, company website.
- States: guest → registered but unpaid «عضوية غير مفعّلة» (message «برجاء تفعيل عضويتك») → annual member (360-day countdown) → admin (promoted in the hub).
- Guests may browse public content: project list and project pages without private fields, the golden portal, and Saudi decisions news.
- In-app account deletion is required by both stores.

## 4. Annual membership (single tier)
- 1,899 SAR per year, promotional «لفترة محدودة», base price 3,900. The in-app store price point is decided later.
- Benefits (server-driven content):
  - Member's project re-posted 3 times a month on club platforms and the app; «شخصية ومسيرة» feature (vcmem.com/famous).
  - Projects Bank credit worth 3,750 SAR = 5 projects.
  - AI advisor with a renewing «رصيد».
  - Online consultation with club experts «بدون رسوم»; priority at ملتقيات and ملتقى «5 دقائق».
  - Riyadh HQ access by booking; club theater (60 seats) and meeting room (8 people, Zoom) at 50%.
  - 50% off: اصنع ملتقاك، Pitch Deck، بودكاست الملتقى studio packages (no usage limit), Momentum website/app design, e-marketing packages, influencer ads, franchise kit (حقيبة امتياز).
  - Up to 20% on «منفذ» products (QDT).
  - «قريبًا»: discounts from وديني القابضة، سكة، وديني سكاي، PV spaces.

### 4.1 Membership benefits page copy (owner, 2026-09-12)
Groups and lines as the owner wrote them (server-driven; studio lines link to WhatsApp +966 53 846 1110):
- **بنك المشاريع**: رصيد 3,750 ريال في بنك المشاريع يعادل 5 مشاريع كاملة تختارها بنفسك · أولوية عرض مشروعك على المستثمرين في بنك المشاريع خلال 3 أيام كحد أقصى · نشر مشروعك على منصات النادي 3 مرات شهريًا على القروبات والمنصات والتطبيق · التقديم على شركاء النجاح بأولوية · تصفّح كل مشاريع بنك المشاريع · التواصل المباشر مع مؤسسي المشاريع.
- **حضورك وأدواتك**: أولوية الحضور في ملتقيات النادي وملتقى «5 دقائق» — حضوريًا في الرياض أو عبر الإنترنت · دخول مقر النادي بالرياض (باركود الدخول يُصدر للأعضاء المشتركين بعد حجز الموعد) · استشارة أونلاين مع خبراء النادي بدون رسوم · مساعد الذكاء الاصطناعي برصيد يومي متجدد على مدار الساعة · ظهورك في «شخصية ومسيرة» بدون رسوم · الانضمام إلى قروبات النادي · حساب واحد لكل مواقع المنظومة · حجز الاستديو (بودكاست الملتقى) للأعضاء المشتركين عبر واتساب الاستديو.
- **خصومات المنظومة**: خصم 50% على مسرح النادي وقاعة الاجتماعات · خصم 50% على خدمات المنظومة (اصنع ملتقاك، Pitch Deck، مومنتوم، بودكاست الملتقى، حقائب الامتياز) · كود خصم حتى 20% على منتجات «منفذ».
- **خصومات قادمة تُضاف لعضويتك تلقائيًا** (كلها «قريبًا»): خصومات شركة وديني القابضة (النقل والشحن والسفر في تطبيق واحد) · خصومات تطبيق سكة (النقل المدرسي التشاركي) · مزايا وديني سكاي (مقاعد الطيران الخاص) · أسعار خاصة في مساحات PV (مساحات ومكاتب العمل داخل المنظومة).
## 5. Version 1 features
1. **Home**: dashboard-driven banners and sections. Each banner has an audience (all / members / unactivated / persona) and start and end dates.
2. **Projects Bank**: search in Arabic and English, sector and stage filters, sort options, project page, unlock for paid members.
3. **Golden projects portal** («المشاريع الذهبية», the projects carrying علامة V): the umbrella plus 10 companies, portfolio value 161M SAR. Disclaimer: «المعلومات تعريفية وليست عرضًا تعاقديًا أو ضمانًا لعوائد».
4. **Membership screen**: benefits and status (store purchase in a later milestone).
5. **AI advisor**: the same hub conversation the member has on the websites; text, voice, image. «اسأل المستشار» sends the current screen's context (project or news item).
6. **Services**: list price and member price. Real-world services paid via Paymob. Studio booking: package and preferred time, handed to the studio WhatsApp. Project registration: شركاء النجاح (pvspaces.com/sp/) → specialised committee review → listed in بنك المشاريع only if approved.
7. **News**: fixed «قرارات وأنظمة المملكة» section for everyone; personalized feed by interests chosen at signup; sports only when there is a business angle (club acquisitions, transfer fees, sponsorships, broadcast rights). Source tiers: Saudi official → trusted Saudi media → global.
8. **HQ visit** (paid members only): date, time and purpose → admin confirmation → QR pass valid for that slot only. No walk-ins.
9. **Account**: profile, photo, digital membership card, delete account.

### 5.5 Owner decisions of 2026-09-16 (closing scope before launch, in the owner's priority order)
The owner's words: «عايزين نقفل كل شيء في التطبيق»; build after the M7 preview build, in this order, then M8 (launch).
1. **«رسائل الإدارة» first** (owner: «دي تكون أول حاجة، رسايل الأدمن مهمة جدًا»): a dashboard section where the admin publishes daily posts with text, links, images and video; the posts are the first block of the app (top of the home), each post can send a push notification. This is the «أخبار النادي» content of the 2026-09-11 decision, now the dashboard's first section.
2. **Neutral first, everywhere** («محايد أول شيء وبعدين رائد الأعمال وبعدين المستثمر، ده في كل شيء»): the persona order becomes محايد → رائد أعمال → مستثمر in the home portals, the registration persona choice, the advisor opening and every list or filter.
3. **The neutral is number one** («اهتم أوي بالمحايد… المحايد عندي رقم 1»): a dedicated page «دليل المحايد» that tells the neutral what to do and where to go (steps with in-app calls to action: browse the Projects Bank, the advisor, the workshops, the membership); neutrals browse the projects (public fields) and benefit from the workshops all year: an in-app workshops page with the schedule and registration (today workshops are a web link in §5.2).
4. **«شخصية ومسيرة» in the app** (the owner also wrote «سيرة ومسيرة»: confirm the section title before building): a section at the beginning of the app with the owner's line «لديك الخبرات والمهارات والمؤهلات والإمكانات التي تؤهلك لتكون إحدى شخصيات …» (the owner completes the sentence); a member applies from the app (form: bio, career milestones, photo, links), gets access to their profile page after the admin approves it in the dashboard; the admin can also enter and edit profiles there (extends the 2026-09-11 decision).
5. **Members' chat**: cancelled by the owner on 2026-09-16 («كنسل فكرة الدردشة دلوقتي خالص»). Not built; no rooms, no voice notes, no direct messages.
6. **Golden offer pages in-app** (2026-09-13, §5.1) stays in the list.
**Dashboard hosting (2026-09-16)**: the owner reserved a dedicated domain that will serve the dashboard only (2026-09-17: `vibesholdingsa.com`, bought at Hostinger, DNS at Hostinger; planned host `dashboard.vibesholdingsa.com` by CNAME; whether the apex can point at Railway from Hostinger DNS is NOT verified, check the record Railway asks for when the domain is added). The dashboard is the `admin/` web app, served at that domain (Railway custom domain on the same project), signing in with hub admin accounts through the same API.
Also from the owner: nothing should require leaving the app; every web link in §5.2 gets an in-app page over time.

### 5.1 Golden companies (order, valuation, offer page)
- V — فايبز القابضة — vibesholding.com/offer/
- 01 — وديني — 31M — wdeny.com/offer/
- 02 — شركة المجتمع الافتراضي (المشغّل الرسمي لنادي المستثمرين) — 30M — vcmem.com/offer/
- 03 — PV لحاضنات ومسرعات الأعمال — 20M — pvspaces.com/offer/
- 04 — سكة — 15M — sekaride.com/offer/
- 05 — الملتقى — 15M — almoltaqapodcast.com/offer/
- 06 — القضمة السريعة — 10M — qbarabia.com/offer/
- 07 — الصفقات السريعة للتجارة — 10M — qdtco.com/offer/
- 08 — مومنتوم — 10M — momentummix.com/offer
- 09 — الصفقات السريعة للاستثمار — 10M — qdealsi.com/
- 10 — تمكين الامتياز — 10M — franchment.com/offer

Logos are in `https://vibesholding.com/wp-content/uploads/2026/08/`: فايبز-القابضة.png, Wdeny-logo-png.webp, لوجو-المجتمع-الافتراضي.png, PV.png, Seka.webp, Al-Moltaqa.webp, القضمة-السريعة.png, الصفقات-السريعة-للتجارة.webp, مومنتوم-scaled.png, الصفقات-السريعة-للاستثمار.png, تمكين-الامتياز.png. Seed them as editable server content; do not hardcode.
- Owner decision (2026-09-13, essential): every golden company's `/offer/` page gets a native in-app page that mirrors the web version (same sections, images and calls to action), so a member never has to leave the app; the golden portal and the company cards open it instead of the browser. To settle when it is built (proposed as the first item after the M7 preview build): the content source, either the offer pages' WordPress content through a feed or REST endpoint kept in sync automatically, or dashboard-entered blocks per company (`content:golden`); the calls to action stay in-app (advisor, services, Projects Bank, WhatsApp handover). The owner's wider rule from the same message: nothing should require leaving the app, so the remaining web links in §5.2 follow the same path over time.

### 5.2 Services and current web links
- Pitch Deck: 5,000 SAR (https://paymob.link/2rXb1); members 2,500 (https://paymob.link/gSyCE). Executed by شركة PV.
- اصنع ملتقاك: 30,000 SAR (vibesholding.com/urmeet/); members 50% (https://paymob.link/vkfVr).
- Studio: prices are kept in the hub settings; page almoltaqapodcast.com/studio/ with anchors #basic #advanced #full #theater #outdoor #addons; members 50%; bookings through the studio WhatsApp.
- Workshops: vcmem.com/workshop/. Current workshop «مشروعك من الفكرة إلى التنفيذ», October 17 to 19, 2026, at the HQ plus online, 290 SAR instead of 1,200 (https://paymob.link/tH3EE), members 50%.
- Any other member service: the management number.

These links are the current web flow. The target in-app flow is server-created Paymob intentions linked to the member's account.

### 5.3 HQ facts
الرياض، شارع الأمير محمد بن عبدالعزيز (التحلية سابقًا)، برج الجوهرة، حي العليا — 400 m², 15 offices, meeting rooms, theater and stage, coffee shop.
Map: https://maps.app.goo.gl/A8JZpNt5AgzC33m56 — Tour video: YouTube `XERenNWNziA`.

### 5.4 Videos (YouTube IDs)
Club story `CnWMWS_nDpM` · 11,000+ members `KUzcLImZc-c` · How the club works `3hvxhZL-gsM` · Club categories `TzeJOrO4nMI` · سكة `vLI-G989nIk` · اصنع ملتقاك `xpxKMnj_Nzk` · ملتقى 5 دقائق `ReJljicbEJc` · HQ tour `XERenNWNziA`

## 6. Brand assets (official)
- Club gold logo (transparent): https://vcmem.com/wp-content/uploads/2025/11/لوجو-نادي-المستثمرين-جوولد-شفاف.png
- شركاء النجاح: https://vcmem.com/wp-content/uploads/2025/08/English-PNG-copy.webp
- بنك المشاريع: https://vcmem.com/wp-content/uploads/2026/09/بنك-المشاريع.png
- فايبز القابضة: https://vcmem.com/wp-content/uploads/2025/11/Vibes-Logo-png-small.webp
- PV: https://vcmem.com/wp-content/uploads/2026/09/PV.png
- Zoom icon: https://vcmem.com/wp-content/uploads/2026/09/zoom-video-meeting-app-icon-free-vector-copy.webp
- Visual identity: premium gold with black and white. The final palette is confirmed with the owner at the design step.

## 7. Environments
- **test** (now): Railway environment `test`; Paymob TEST secret and public keys with TEST integration IDs; preview builds only.
- The hub and the Projects Bank exist only as live systems. In test, the server reads them read-only. Never trigger a real membership activation, a real payment, or e-mails to non-admin users. Test login with existing admin accounts only; do not create registrations on the live hub without the owner's approval.
- **production**: only after the owner's explicit go-live.

## 8. Milestones
- [x] **M1 — App shell and first APK** (no backend): Expo app in `app/` with TypeScript, expo-router, forced RTL, IBM Plex Sans Arabic and theme tokens. Bottom tabs: الرئيسية، بنك المشاريع، المستشار، الأخبار، حسابي, each a placeholder screen in Arabic with the club logo. `APP_ENV` config and app variants (preview and production ids), eas.json profiles, EAS Update configured. Build the preview APK and give the owner the install link. Also `.gitignore`, a README with setup steps, and the first push to the private GitHub repo.
  - Status (2026-09-11): done. Expo SDK 57 app in app/ (expo-router in src/app, tabs, forced RTL via expo-localization plugin, IBM Plex Sans Arabic, tokens in src/theme/tokens.ts, app.config.ts with APP_ENV variants, eas.json, EAS Update channel preview created). First preview APK: build 1258df03 on the EAS dashboard (builds page of @vibes-holding/vibes-holding). Repo: github.com/Yasserpsd/Vibes-Holding-APP. Open items for M2: set EXPO_PUBLIC_API_URL per EAS profile once the server exists; confirm final palette with the owner; verify RTL and fonts on a real device.
- [x] **M2 — Server and read-only content**: server skeleton on Railway `test`; Projects Bank in the app (list, search, filters, project page without private fields); golden portal from seeded server content.
  - Status (2026-09-11): done and deployed. Railway project "faithful-nurturing", service Vibes-Holding-APP (root directory `server`, auto-deploys from main), environment still named production on Railway but APP_ENV=test; public URL https://vibes-holding-app-production.up.railway.app (health shows 260 projects synced and the feed key fingerprint 40:3cfb9a9e, which matches the owner's server/.env). Variables on Railway: APP_ENV, DATABASE_URL (${{Postgres.DATABASE_URL}}), HOST, LOG_LEVEL, PB_FEED_KEY, PB_FEED_URL, PB_REFRESH_MINUTES. app/.env and eas.json (development, preview) point at that URL. server/ = Fastify 5 + TypeScript + Zod + pg (Node 24, ESM); key/value table `kv` in Postgres with in-memory fallback when DATABASE_URL is empty; golden portal seeded at boot (server/src/content/golden.ts, key `content:golden`); Projects Bank synced every PB_REFRESH_MINUTES from the vibesholding feed, mapped to a public whitelist (server/src/projectsBank/mapper.ts) before caching, so whatsapp/email/website/pitch_deck never leave the server. Endpoints: /health, /api/projects (q, sector, stage, sort=latest|views|discover|golden, page, limit), /api/projects/filters, /api/projects/:id, /api/golden; all /api/* answers carry Cache-Control: no-store. 11 node:test tests pass (npm test). App: TanStack Query + expo-web-browser (lazy-loaded with a system-browser fallback because the M1 APK lacks the native module); screens src/app/(tabs)/projects.tsx, src/app/project/[id].tsx, src/app/golden.tsx; home links to both. Feed verified with the key in server/.env: plugin v38.1, 260 projects, envelope {ok,count,generated,rules,projects[]}, each item has top-level id/number/title/title_en/url/image/sector/project_stage (strings)/golden/partner_url/contact_rule (golden_direct|members_only)/modified plus meta (company_name, founder_name, project_details plain text, excerpt, is_featured, has_pitch_deck, views_count); the feed already omits whatsapp/email/website/pitch_deck, the server still whitelists. The feed `rules` text carries web membership prices and is never exposed. Open items for M3: the owner runs `eas update --channel preview` from app/ (OTA, no new build) and checks the new screens, RTL and fonts on the M1 APK; confirm /health reports storage "postgres" on Railway (else fix the DATABASE_URL reference); the next native build must include expo-web-browser (in-app browser) and expo-secure-store.
- Content decision (2026-09-11): «شخصية ومسيرة» member profiles and «أخبار النادي» club news are entered manually from the admin dashboard (server-driven content), not synced from WordPress; the Projects Bank stays automatic (feed); the AI advisor is connected to both (owner's words: «AI هو اللي يكون مربوط بالاثنين»; confirm the exact meaning when M4 starts). This content milestone comes after M3 (the owner chose to start M3 first).
- [x] **M3 — Real accounts**: hub bridge → registration, OTP and login in the app; membership status; preview login limited to admins.
  - Status (2026-09-11): merged (PR #1) and deployed; Railway runs the mock hub until HUB_SITE_KEY is set. The first two M3 OTA updates closed the app on the M1 APK: a lazy require() of expo-secure-store (missing from that binary) is reported as a fatal JS error before try/catch runs. Fixed in commit 3bdd954 (requireOptionalNativeModule gate for ExpoSecureStore and ExpoWebBrowser), reproduced and verified on the local Android emulator by embedding the exported bundle in the M1 APK, and published as OTA group 26befa11. That update also shipped without the API URL (eas update --environment preview did not inline app/.env): fixed by storing EXPO_PUBLIC_API_URL in the EAS preview and development environments and by a test-only fallback in app.config.ts (commit 584ffdd). Verified end to end on the emulator against Railway (mock hub): register → code 123456 → account, active membership card, membership screen, profile edit, logout, login by phone, delete account (prompt fixed to stay above the keyboard, commit 5d7a3d7). Latest preview update group 563f1731. Local emulator recipe in the auto-memory note ota-native-module-crash. The hub bridge is the hub's existing tenant-site API (`POST /wp-json/vibes-ai/v1/hub/{op}` with `X-VAI-Site-Key`; the app server is a site of the hub, one hub uuid per app session) — full contract and owner steps in `docs/HUB_BRIDGE.md`. Server: `server/src/hub` (live client + mock), `server/src/auth` (sessions hashed in `kv`, service, routes), `/api/auth/config|register|resend|verify|login|reset/*|logout`, `/api/me` (GET/PATCH/DELETE), `/api/membership` (seeded benefits, no prices); env `HUB_MODE`, `HUB_URL`, `HUB_SITE_KEY`, `HUB_ALLOW_TEST_REGISTRATION`, `SESSION_DAYS`; test rules: admin-only login and registration closed on the live hub; 21 node:test tests pass. Plugin: Vibes AI Assistant 2.4.1 = 2.4.0 + op `delete_account` (zip placed next to the original on the owner's D: drive; diff in `docs/hub-plugin/`). App: `src/auth` (AuthProvider, secure-store token with in-memory fallback for the M1 APK), screens `auth/login|register|verify|reset`, `membership`, `profile-edit`, the account tab and a membership link on home; `expo-secure-store` added. Open items: owner pushes (Railway redeploys in mock mode: code 123456, every account admin) and runs `eas update --channel preview`; owner creates the app site in the hub «المواقع» and sets `HUB_SITE_KEY` + `HUB_MODE=live` on Railway, then tests login with a hub admin account; owner uploads plugin 2.4.1 (needed only for «حذف الحساب»); the next preview APK (needs the owner's OK) must be built so sessions persist (expo-secure-store) and the in-app browser works.
- [x] **M4 — AI advisor**: chat through the hub, same conversation as the websites.
  - Status (2026-09-12): implemented on branch `m4-advisor` (owner pushes and merges; not verified on a device yet). Owner's clarification of «AI مربوط بالاثنين»: one conversation per account, on vcmem.com and in the app. The hub already stores messages per contact (not per site), so `history` / `poll` / `message` with the session's hub uuid return the thread the member sees on the websites; no plugin change was needed (ops read from plugin 2.4.1 source, `docs/HUB_BRIDGE.md` §2). Server: `server/src/advisor/` (service, sanitize, routes), `server/src/hub/mockChat.ts` (mock chat: canned reply after 1.5 s carrying the web widgets, so the filtering is exercised locally and on Railway while HUB_MODE=mock), shared `server/src/auth/guard.ts` (guard / parse / sessionGuard). Endpoints: `GET /api/advisor/history`, `POST /api/advisor/message` (`text`, optional `context { type: 'project', id }` → `page_title` + `page_url` from `ProjectsService.pageUrl()`, kept server-side in the feed snapshot; the web URL never reaches the app), `GET /api/advisor/poll?after=<id>`. Rule 3 enforced in `sanitize.ts`: membership card → `{ type: 'membership' }` action (in-app membership screen), membership / Paymob / checkout / pay links stripped from text and buttons, hub gate texts replaced by app texts, web-only widgets dropped. 28 node:test tests pass. App: tab «المستشار» = `app/src/components/advisor/` (useAdvisorChat: history, optimistic send, polling every 2 s while the workflow answers and every 15 s otherwise while focused; AdvisorChat, MessageBubble with quick replies / links / videos / service cards / staff replies, Composer, RichText for **bold** and tappable links), guest view with login, day separators, daily «رصيد» in the header from `me.membership.aiDailyLeft`; «اسأل المستشار عن هذا المشروع» on the project page opens the tab with the project as a topic chip (sent as context until dismissed). Voice and image deferred by the owner: the hub `message` op already accepts `audio` / `image` data URLs; they need expo-audio + expo-image-picker and a new APK. Open items: owner pushes `m4-advisor` and merges (Railway redeploys in mock mode until `HUB_SITE_KEY` is set), then runs `eas update --channel preview`; with `HUB_MODE=live`, sign in with a hub admin account and check that the same messages appear on vcmem.com and in the app; add the hub directive for the app site (`docs/HUB_BRIDGE.md` §5 step 6) so the workflow stops quoting web membership prices in its replies; the iOS keyboard offset in AdvisorChat assumes a 49 pt tab bar (check on the first iOS build).
- [x] **M5 — News engine**: source polling, AI classification, fixed Saudi decisions section, personalized feed.
  - Status (2026-09-12): implemented on branch `m5-news` (branched from `m4-advisor`; the owner pushes and merges both) and verified on the local Android emulator with a local debug build. Server: `server/src/news/` — `sources.ts` (seeded list at kv `content:news:sources`, dashboard-editable later; working sources: SPA portal API by category (economic, general, political, tourism, tech) through `spa.ts` (the agency has no RSS; `portalapi.spa.gov.sa/api/v1/news?category_id=N` with `accept-language: ar` lists the site's items, the article page `www.spa.gov.sa/ar/<uuid>` is still fetched for verification and the snippet is the agency's own opening paragraph from `/news/<uuid>`), Okaz economy + local RSS, Asharq Al-Awsat economy RSS, Al Eqtisadiah through Bing News RSS (`site:aleqt.com`, links unwrapped to the original URL), BBC Arabic business; seeded disabled: Al Arabiya (403 to non-browser TLS), Argaam (page timeouts), Maaal (403), Saudi Gazette and Arab News (English)); `rss.ts` (RSS 2.0 / Atom / RDF, Bing links, tracking parameters stripped); `page.ts` (rule 7: every item's URL is fetched with a browser user agent before the item exists; og:title/description/image/site_name as fallbacks only); `classify.ts` (keyword classifier: Arabic word-boundary patterns, 11 topics, decision = act verb + instrument noun + authority + Saudi signal, sports only with money terms, protocol/aid/weather noise hidden); `openai.ts` (chat completions with a strict JSON schema, batches of 20, per-batch fallback to keywords; active when `OPENAI_API_KEY` is set, `OPENAI_MODEL` default `gpt-4.1-mini`); `dedupe.ts` (title-token Jaccard ≥ 0.6 within 72 h, higher tier wins); `service.ts` (poll every `NEWS_REFRESH_MINUTES` = 20, `NEWS_MAX_NEW_PER_RUN` = 200, `NEWS_MAX_AGE_DAYS` = 10, snapshot at kv `news:snapshot`, interests at `news:prefs:<contactId>`, ranking = tier + recency (36 h half-life) + relevance + interest match, persona defaults until the member picks). Endpoints: `GET /api/news/topics`, `GET /api/news/feed?topic&page&limit` (optional bearer token → personalized), `GET /api/news/decisions`, `GET|PUT /api/news/prefs` (session), `GET /api/news/:id`; `/health` → `news` (counts, classifier mode, per-source ok/count/stored/failed). The advisor context now also accepts `{ type: 'news', id }` (title + URL as the page context). 40 node:test tests pass. First real run (keyword mode): 10 sources ok, about 120 verified items, about 68 visible, 5 decisions. App: tab «الأخبار» (`src/app/(tabs)/news.tsx`: decisions strip + «عرض الكل», interests banner, topic chips with the member's interests first, personalized feed), `news/[id]` (source, tier label, time, title, snippet, «اقرأ من المصدر» in the in-app browser, «اسأل المستشار عن هذا الخبر», a note that the text is the source's own), `news/decisions`, `news/interests` (multi-select with the persona's suggestion), interests chips on the register form saved right after the code is verified, `components/news/NewsCard`, `api/news.ts`, Arabic relative time in `lib/format.ts`. Local emulator recipe without EAS (details in the auto-memory note `local-android-debug-build`): `npx expo prebuild --platform android` (android/ is git-ignored) + `gradlew assembleDebug -PreactNativeArchitectures=x86_64`, install the debug APK, `adb reverse tcp:8081 tcp:8081`, Metro with `EXPO_PUBLIC_API_URL=http://10.0.2.2:3000` against the local server (mock hub, real feeds). Verified on the emulator: register with interests → code 123456 → personalized news tab → interests screen → item detail → advisor with the news topic (the mock reply quotes the title) → in-app browser; the M4 advisor chat (send, reply widgets, quick replies) verified on the device as well. Open items: owner pushes `m4-advisor` and `m5-news` and merges; owner sets `OPENAI_API_KEY` (and optionally `OPENAI_MODEL`) on Railway so the real classifier runs (keyword mode is the fallback and is noisier); owner runs `eas update --channel preview` (JS only, no new native module); the dashboard (M7) edits `content:news:sources`; consider AI-assisted dedupe of the same story across outlets (token similarity only today); the iOS keyboard note from M4 stays open.
- [x] **M6 — Home portals, membership benefits, services, about, video library, HQ**: the owner's requirements from 2026-09-12 (his words summarized):
  - **Home = three portals, «شيك جدًا» (premium look)**: (1) «بوابة المستثمر» → opens the Projects Bank; (2) «بوابة رواد الأعمال» → a page with three services: «اصنع ملتقاك», «شركاء النجاح» (project registration → committee review → listed in the bank only if approved; pvspaces.com/sp/), and «تصميم Pitch Deck»; (3) «بوابة المحايدين» → opens the AI advisor, which starts the conversation itself and asks the visitor about their company/direction (needs a hub directive or an opening message; the hub already stores one conversation per account). Also on home: «المشاريع الذهبية من فايبز القابضة» and the membership («العضوية الذهبية» is the owner's wording on 2026-09-12; the brief calls it the annual membership — confirm the name the app should show).
  - **Services shown but locked**: the app lists many services; for guests and unactivated accounts they appear dimmed («مطموسة») with the message that the annual membership must be activated to use them. Studio booking («بودكاست الملتقى») is a paid-member benefit and every studio action hands over to WhatsApp +966 53 846 1110 (Paymob stays for real-world services per M6's original scope; confirm which services are paid in-app in the test environment).
  - **Membership benefits page copy** (server-driven content, replaces the current `content/membership.ts` seed; keep the owner's wording, the «بدون رسوم» rule and no membership price): see §4.1 below.
  - **About page («عنّا»)**: club, فايبز القابضة ecosystem and شركة المجتمع الافتراضي للاستثمار, written from the official sites (vcmem.com, vibesholding.com) — the owner gives the OK to read those sites in the M6 session; content stored server-side so the dashboard edits it later.
  - **Video library**: every video of the YouTube channel «نادي المستثمرين» https://www.youtube.com/@investorscl/videos, each with a short friendly Arabic blurb; new uploads appear in the app automatically. Implementation notes: the channel's public RSS (`https://www.youtube.com/feeds/videos.xml?channel_id=…`) lists the latest 15 uploads without a key; the full back catalogue needs the YouTube Data API v3 (free quota, key in env only) or a one-time seed from the RSS + dashboard; blurbs are marketing copy (not news, rule 7 does not apply) generated once and editable from the dashboard; play in-app with the existing YouTube handling from M4 (video widgets) or a WebView.
  - **HQ visit** (original M6 scope, paid members): date, time, purpose → admin confirmation → QR pass for that slot.
  - Order suggested for the session: home portals + locked services + benefits copy first (one OTA), then videos + about, then HQ booking; Paymob test payments last.
  - Status (2026-09-12): implemented on branch `m6-home` (the owner pushes and merges; Railway redeploys) and verified on the local Android emulator with the M5 debug APK (JS only, no new native module, OTA-safe). Server: content seeds are versioned (`version` field; `ensure*Seed` overwrites an older stored version at boot; `npm run seed -- --force` rewrites everything): `content:home` (`server/src/content/home.ts`: hero, the three portals, golden / membership / services / videos blocks, the entrepreneurs page service keys, the neutral-portal advisor opening), `content:services` (`services.ts`: 15 services in 3 groups with price and member labels, `access: everyone | member` and one action each — WhatsApp handover with form fields to the management number +966 55 831 8777 or the studio +966 53 846 1110, web link, advisor, HQ, Projects Bank; `GET /api/services` and `/api/services/:key` lock member-only services for guests, unactivated and expired accounts and strip their action server-side), `content:about` (`about.ts`, written from vcmem.com and vibesholding.com), `content:hq` (`hq.ts`: address, map, tour video, facilities, rules; hours Sunday to Thursday 10:00 to 17:00 in 60-minute slots as a working assumption, lead 1 day, 30 days ahead, capacity 5 per slot, purposes), `content:videos` (curated picks from §5.4), `content:membership` v2 = the owner's §4.1 groups with route / WhatsApp links, title «العضوية الذهبية» (legacy flat `benefits` / `comingSoon` kept for older app JS). Videos: `server/src/videos/` — the handle `@investorscl` is resolved to the channel id from the channel page, the public feed lists the latest 15 uploads, `YOUTUBE_API_KEY` (optional, env) lists the full catalogue through the uploads playlist, known videos are kept when the feed drops them, one friendly line per video written once (OpenAI when `OPENAI_API_KEY` is set, else a template from the title or description) and stored at `content:videos:blurbs` for the dashboard; `GET /api/videos?page&limit`, `GET /api/videos/:id`, `/health` → `videos`; `VIDEOS_REFRESH_MINUTES` default 60. HQ: `server/src/hq/` — `GET /api/hq` (content, access, bookable days, slot times), `GET /api/hq/slots?date`, `GET | POST /api/hq/visits`, `POST /api/hq/visits/:id/cancel`, `GET /api/hq/visits/:id/pass` (QR PNG data URL from the `qrcode` package, content `VCHQ:<visit>:<code>`, valid from 15 minutes before the slot until its end); admin (hub admin accounts): `GET /api/admin/hq/visits?status`, `POST /api/admin/hq/visits/:id/decision`, `POST /api/admin/hq/verify`; visits live in kv `hq:visits` (in-process write lock, pruned after 90 days). Advisor context accepts `{ type: 'portal', id }` and `{ type: 'service', id }` (the screen title becomes the page context). `optionalSession`, `adminGuard` and `RequestError` in `server/src/auth/guard.ts`. 52 node:test tests pass. App: home (`src/app/(tabs)/index.tsx`: hero, three `PortalCard`s, golden strip, membership block, services teaser, featured videos strip, HQ and about links), `portal/entrepreneurs`, `services/index` + `service/[key]` (`ServiceCard` dims locked services with a «للأعضاء» lock, `LockedNotice`, WhatsApp request form through `lib/whatsapp.ts` and a `wa.me` link), `videos/index` (`VideoCard`, plays in the in-app browser), `about`, `hq/index` + `hq/book` + `hq/pass/[id]` + `hq/admin` (admin accounts only, also linked from the account tab), `membership` rewritten for the groups; advisor tab: the neutral portal shows the server-driven opening bubble with quick replies until the member answers (`ChatOpening` in `AdvisorChat`), services can open the advisor with a suggested first message. Verified on the emulator: home → portals → entrepreneurs page → services (guest: locked and dimmed with the notice; member: unlocked) → studio request form → login (mock, `+member` account) → HQ → book (day, slot, purpose) → admin confirm → QR pass → membership groups → videos (15 real uploads plus the curated picks) → about → the neutral-portal opening in the advisor. Open items: owner pushes `m6-home` and merges (the versioned seeds rewrite `content:membership` at boot, the new keys are seeded), then `eas update --channel preview` (JS only); decide which real-world services are paid in-app through Paymob in the test environment (not built in M6: every paid service hands over to WhatsApp with a prefilled request today; a `paymob` variant of `ServiceAction` plus the intention / HMAC callback work follows the decision, rules 5 and 6); optionally set `YOUTUBE_API_KEY` on Railway for the full back catalogue and `OPENAI_API_KEY` for AI-written video lines; confirm the HQ hours and capacity (`content:hq`) and the membership name (`content:home`, `content:membership`); the dashboard (M7) edits every `content:*` key and takes over the HQ admin screen; the WhatsApp handover could not be exercised on the emulator (no WhatsApp installed), check it on a real device.
- [ ] **M7 — Store accounts**: iOS test builds (TestFlight or ad hoc), push notifications, membership IAP via RevenueCat, admin dashboard.
  - Status (2026-09-12, part 1, branch `m7-payments` branched from `m6-home`; the owner pushes and merges both): owner's decisions of this session: one membership name «العضوية السنوية لنادي المستثمرين», no golden tier (`content:home` v2, `content:membership` v3); HQ hours Sunday to Thursday 10:00 to 19:00 (`content:hq` v2, 9 slots); the two services paid in-app through Paymob are Pitch Deck (5,000 SAR, members 2,500) and «اصنع ملتقاك» (30,000, members 15,000) (`content:services` v2: `ServiceAction` variant `paymob` with `amount`, `memberAmount`, `currency`; `GET /api/services` adds `price` for the account); the annual membership itself is sold only through the stores (rule 3, RevenueCat): server side ready, SDK and build pending; every action e-mails the management like the website does, from the vcmem.com hosting mailbox. Server: `server/src/mail/` (`mailer.ts`: nodemailer SMTP from `SMTP_HOST`, `SMTP_PORT`, `SMTP_SECURE`, `SMTP_USER`, `SMTP_PASS`, `MAIL_FROM`, log-only when unset; `notify.ts`: `Notifier`, plain-text Arabic mails in the website's style, subject prefix `[Vibes AI]` plus `[تجريبي]` in test, Riyadh time, recipients = `NOTIFY_EMAIL` comma list) for HQ visit requested / cancelled / confirmed / rejected (`HqService` takes the notifier, `decide()` takes `{ id, name }`), a service request before a WhatsApp handover (`POST /api/services/:key/request`, the app calls it fire-and-forget), payment started / paid / failed, store membership activated / needs manual activation. Payments: `server/src/payments/` — `paymob.ts` (`LivePaymob`: Intention API `POST {PAYMOB_BASE_URL}/v1/intention/` with `Authorization: Token <secret>`, `special_reference` = payment id, per-payment `notification_url` `/api/payments/paymob/webhook` and `redirection_url` `/pay/return?payment=<id>` built from `PUBLIC_URL` (Railway: `RAILWAY_PUBLIC_DOMAIN` fallback), checkout URL `{base}/unifiedcheckout/?publicKey&clientSecret`; `MockPaymob` when the four `PAYMOB_*` values are missing (`PAYMOB_MODE` auto, refused in production): local page `/pay/mock/:id` that fires the same HMAC-signed callback), `hmac.ts` (SHA-512 over Paymob's fixed field order, callback and redirect), `service.ts` (kv `payments:orders`; `start()` needs a session, an unlocked `paymob` service, member price by membership; `handleWebhook()` is the only path that sets `paid` / `failed`, rule 5, amount must match, idempotent; `redirectResult()`; `mockComplete()`), `routes.ts` (`POST /api/payments`, `GET /api/payments`, `GET /api/payments/:id`, `POST /api/payments/paymob/webhook?hmac=`, `GET /pay/return` = Arabic HTML with the deep link `investorsclub[-preview]://payment/<id>`, `GET /api/admin/payments?status`), `pages.ts`.
 Store membership: `server/src/membership/` — `POST /api/webhooks/revenuecat` guarded by the `Authorization` header (= `REVENUECAT_WEBHOOK_AUTH`, 503 until set); INITIAL_PURCHASE / RENEWAL / NON_RENEWING_PURCHASE / UNCANCELLATION / PRODUCT_CHANGE activate through the hub op `activate_member` (`app_user_id` = `vc-<contact id>`, days from the event, default 365); the mock hub implements the op, the live plugin does not yet (docs/HUB_BRIDGE.md §6): until a plugin release adds it, the mail «عضوية من المتجر تحتاج تفعيلًا يدويًا» asks the management to activate by hand; events at kv `membership:purchases`, `GET /api/admin/membership/purchases`; `AuthService.forget(contactId)` drops the cached account so `/api/me` shows the new status at once. `/health` → `mail`, `payments`, `membership`; every variable is documented in `server/.env.example`; 69 node:test tests pass (`m7.test.ts`). App (JS only, OTA-safe): `api/payments.ts`, `lib/checkout.ts` (`openAuthSessionAsync` with the app-scheme return URL; the status is always read from the server), `service/[key]` (paymob panel: fields, the price for this account, «ادفع الآن» or a login button; WhatsApp actions report the request first), `payment/[id]` (polls every 3 s while `created`, reopen checkout, retry), `payments/index` («مدفوعاتي», linked from the account tab), `hq/pass/[id]` redrawn as a ticket (gold header with the club logo, holder name, visit day and time, purpose, booking date and time, tear line with notches, QR stub, state pill; `Pass.name` added), `formatArabicDateTime` in `lib/format.ts`. Verified on the local emulator (M6 debug APK + Metro + local server in mock mode, `PUBLIC_URL=http://10.0.2.2:3001`): login → services → Pitch Deck (2,500 for the member) → «ادفع الآن» → mock gateway page in the in-app browser → «ادفع الآن (نجاح)» → return page → «العودة إلى التطبيق» deep link → payment screen «تم الدفع بنجاح» → «مدفوعاتي»; HQ booking 18:00 to 19:00 → admin confirm → ticket pass; the server log shows every notification (logged only: no SMTP locally). Open items: owner pushes and merges `m7-payments`, then `eas update --channel preview`; owner sets on Railway `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `MAIL_FROM`, `NOTIFY_EMAIL` (vcmem.com hosting mailbox) and the Paymob TEST values `PAYMOB_SECRET_KEY`, `PAYMOB_PUBLIC_KEY`, `PAYMOB_HMAC_SECRET`, `PAYMOB_INTEGRATION_ID` (until then Railway runs the mock gateway too), then checks a real Paymob test payment on a device and the callback in the Railway log; membership purchase (next session): owner creates the Google Play Console app with an annual subscription, the App Store Connect app with the same subscription, and a RevenueCat project (entitlement `membership`, product `club_membership_annual`, webhook URL `/api/webhooks/revenuecat` with the `REVENUECAT_WEBHOOK_AUTH` value), then the app gets `react-native-purchases` (gated like expo-secure-store) and a new preview build (owner's OK), and the plugin gets the `activate_member` op; iOS test build, push notifications and the dashboard stay in M7. Regression pass on the local emulator at the end of the session (2026-09-12, debug APK + Metro + local server in mock mode, `npm test`: 57 pass, 0 fail): every flow from M1 to M7 part 1 works (login, home, services and the two Paymob services with success / failure / abandoned-then-reopened checkouts, «مدفوعاتي», HQ booking with validation, admin confirm, ticket pass and admin code check, studio WhatsApp handover with the management notification, membership, videos, about, portals, golden, profile edit, news → source → advisor with the topic, Projects Bank, logout, guest views, registration with the test code, logged-in non-member views). Gaps found, to fix at the start of the next session: (1) the Paymob service form fields (project name and stage, meetup topic and date) are optional in the app and on the server, so «ادفع الآن» starts a checkout with empty answers and the management mail carries no details; the retry button also returns to an empty form: require the fields before paying and keep the answers on retry; (2) `app/src/lib/format.ts` relative time prints «قبل 2 ساعتين» / «قبل 1 أيام»: use the Arabic dual and singular without the numeral (ساعة، ساعتين، يوم، يومين); (3) the admin barcode check result prints the ISO date (`2026-09-14 14:00`) instead of the Arabic date format; (4) the club logo in the gold ticket header is low-contrast (cosmetic); (5) content: the workshop card says «290 ريالًا بدلًا من 1,200» next to «خصم 50% للأعضاء», which do not add up (owner decides the wording in `content:services`); (6) the WhatsApp handover still needs a real device (the emulator has no WhatsApp: wa.me opens the Play Store).
  - Status (2026-09-13, part 2 session A, branch `m7-payments`; the owner pushes with `git push origin m7-payments m7-payments:main`, Railway deploys `main`): Paymob KSA: the company's live account (MID 28390, Vibes Holding, ksa.paymob.com; live integrations 33090 MIGS card, 33089 Apple Pay, 33088 Madfu, never touched) got a TEST-mode Online Card integration 33439 with the app's webhook and return URLs; Railway holds the test keys (`sau_sk_test_` / `sau_pk_test_` prefixes, verified through `/health` → `payments.secret/public/hmacLength/baseUrl/integrations`, values never in chat) and the server runs `payments.mode: live`; but `POST /v1/intention/` answers 404 «Integration ID does not exist in our system» for 33439, for the live ids and even for `payment_methods: ["card"]` (reproduced from a local server with the same values; the request matches Paymob's official Postman collection) → the owner opened a Paymob support request; until they fix the account, `PAYMOB_MODE=mock` on Railway keeps the in-app flow testable. Server additions: `RequestError.details` (guard.ts) + `LivePaymob.exposeGatewayErrors` (test env only) put the gateway status and body in the 502 so failures can be read from the response; `parseIntegrationIds` accepts method names (`card`, `apple_pay`, `stc_pay`) next to ids; `m7.test.ts` type-checks (the Railway build runs `tsc` over tests too, this had blocked every deploy since M6). Railway facts learned: the hub is the MOCK (`HUB_MODE` unset, no `HUB_SITE_KEY`: any e-mail with `+member@` registers as an active member/admin, code 123456) and storage is `memory` (no `DATABASE_URL`): accounts, payments and visits vanish on every deploy — both to fix (create the tenant site in the hub «المواقع» page → `HUB_SITE_KEY`, `HUB_MODE=live`; attach Postgres). App: OTA updates published to the preview channel (runtime 1.0.0, EAS build 1258df03 of 2026-09-11 on the owner's phone receives them; the in-app browser and secure store are gated, so the M1-era binary uses the system browser for checkout); gaps 1–4 fixed: all fields of a paid service are required before «ادفع الآن» (app check + server 400 `invalid` naming the field), answers carry `key` and the retry button reopens the service with `?answers=<json>`; `formatRelativeTime` uses the Arabic singular/dual (قبل ساعة، قبل ساعتين، قبل 5 ساعات، قبل 15 ساعة); the admin barcode result shows the Arabic weekday and date; the ticket logo sits on a black badge. `npm test`: 57 pass. Next session (part 2 B): membership purchase (owner creates Google Play Console + App Store Connect subscription `club_membership_annual` and a RevenueCat project with entitlement `membership`, webhook `/api/webhooks/revenuecat` + `REVENUECAT_WEBHOOK_AUTH`; note Play products are per package name, the preview build is `com.vcmem.investorsclub.preview`), `react-native-purchases` gated like expo-secure-store + purchase/restore UI on `membership`; push notifications (`expo-notifications`, token registry, sends on HQ decision / payment result / membership activation); then ONE preview build (owner's OK); dashboard and iOS build after. Also open: Paymob support outcome, live hub key, Postgres, gap 5 wording in `content:services`, WhatsApp handover on a real device.
  - Status (2026-09-13, part 2 B, branch `m7-payments`; the owner pushes with `git push origin m7-payments m7-payments:main`, Railway deploys `main`): store membership purchase and member push notifications are implemented and verified on the local emulator; no EAS build was made (owner's OK needed: this is the first change since M1 that needs a new binary, because `react-native-purchases` and `expo-notifications` are native). Server: `GET /api/membership/store` (public RevenueCat SDK keys from `REVENUECAT_PUBLIC_KEY_ANDROID` / `REVENUECAT_PUBLIC_KEY_IOS`, `STORE_MEMBERSHIP_PRODUCT` default `club_membership_annual`, `REVENUECAT_ENTITLEMENT` default `membership`, `STORE_TERMS_URL` / `STORE_PRIVACY_URL`, app user id prefix `vc-`); `POST /api/membership/sync` (session): drops the cached account and, when `REVENUECAT_SECRET_KEY` is set, reads the subscriber from RevenueCat's REST API v1 and activates an active entitlement the webhook has not activated yet (recorded as a `SYNC` event); activation is idempotent (a repeated webhook event id answers from the record; a second activation for the same contact with the same expiry or transaction id is recorded as `duplicate`, so the hub is never asked twice for one purchase). Push: `server/src/push/` (`service.ts`: kv `push:tokens`, one entry per Expo token owned by the account that last signed in on that device, pruned after 120 days; sends through `https://exp.host/--/api/v2/push/send` in chunks of 100 with `channelId: default`, optional `EXPO_PUSH_ACCESS_TOKEN`; `DeviceNotRegistered` tickets drop the token; push receipts are not polled yet); Arabic member messages for the HQ decision (confirmed → `/hq/pass/<id>`, rejected → `/hq`), the payment result (`/payment/<id>`) and the membership activation, also the manual-activation case (`/membership`); routes `POST` / `DELETE /api/push/tokens` (session), `GET /api/admin/push/tokens` (counts), `POST /api/admin/push/test` (sends to the admin's own devices or to `contactId`); `/health` → `push` and `membership.store`; every variable is in `server/.env.example`. `m7b.test.ts` (store settings, token registry, HQ and payment pushes, webhook retry and duplicate guard, sync through a stubbed REST API): `npm test` 61 pass. App (`expo-notifications` ~57.0.18, `react-native-purchases` ^10.9.1): both modules are gated like expo-secure-store (`lib/purchases.ts` checks `NativeModules.RNPurchases`, `lib/notifications.ts` checks `ExpoPushTokenManager` + `ExpoNotificationsEmitter` before requiring), so the JS is OTA-safe on the current preview binary (there the membership screen says this version cannot buy from the store yet and no push token exists). `components/StorePurchaseCard` on `membership` (signed-in accounts): configures RevenueCat with the server keys and `vc-<id>`, shows the store price (`priceString`) with the auto-renewal text, the test-environment note and the terms/privacy links, «اشترك الآن» → store sheet → `POST /api/membership/sync` every 3 s up to 8 times → «تم تفعيل عضويتك» or the pending notice («سنرسل لك إشعارًا»), «استعادة المشتريات»; active members only get «إدارة الاشتراك من المتجر» when the store knows a management URL; sign-out calls `Purchases.logOut()`. `components/PushRegistrar` (root layout): after login asks permission, creates the Android channel `default`, registers the Expo token (`POST /api/push/tokens` with the EAS `projectId`), routes notification taps through `data.screen` (cold start included) and refreshes the account / payments / HQ queries behind foreground arrivals; sign-out unregisters the token before clearing the session. `app.config.ts`: the `expo-notifications` plugin (white club mark `assets/images/notification-icon.png` generated from the logo, gold accent, channel `default`) and `android.googleServicesFile` from the `GOOGLE_SERVICES_JSON` variable (an EAS file variable on builds, git-ignored locally; nothing when unset).
 Verified: `npm test` 61 pass, server and app typecheck and lint pass; a fresh local debug build with the two native modules (`npx expo prebuild --platform android --clean --no-install` + gradle, 3.5 min) installs and starts on the emulator (Metro's first start after `--clean` crawls and bundles for several minutes: wait, do not restart it); on the emulator (mock hub, mock Paymob, a dummy RevenueCat Android key, no Firebase): login creates the notification channel «إشعارات النادي» (importance MAX) with the permission granted, `getExpoPushTokenAsync` fails without Firebase and the app goes on without a token (no JS error, no request); the membership screen shows the status card, then the store card in its «الاشتراك غير متاح في المتجر حاليًا» state with «استعادة المشتريات», the auto-renewal text, the test-environment note and the terms link; «استعادة المشتريات» on the emulator answers «الشراء غير مسموح به على هذا الجهاز أو الحساب» (no Play billing there); sign-out logs RevenueCat out and returns the account tab to the guest state. One bug found and fixed on the way: `setNotificationChannelAsync` refuses `sound: 'default'` (expo-notifications treats it as a custom sound file) and that error had aborted the permission step. The store price and the purchase sheet themselves need a real RevenueCat key and a Play build: Play billing does not exist on the emulator. Emulator note: Metro's first bundle after `prebuild --clean` took 19 minutes and its file watcher never fired; a restart (`taskkill` + `expo start`) listened in 5 s and bundled in 1 s from the transform cache, so restart Metro when the first bundle exceeds a few minutes. Owner's steps before the preview build (all outside the code): (1) Firebase project → Android app with package `com.vcmem.investorsclub.preview` → `google-services.json` → `eas env:create --environment preview --name GOOGLE_SERVICES_JSON --type file --value ./google-services.json --visibility secret`, and the FCM V1 service-account JSON under `eas credentials` → Android → Push Notifications (without it the build gets no push token); (2) RevenueCat project with an Android app for the same package, entitlement `membership`, product `club_membership_annual` attached to the current offering, webhook `https://vibes-holding-app-production.up.railway.app/api/webhooks/revenuecat` with the Authorization header value = Railway `REVENUECAT_WEBHOOK_AUTH`; Railway gets `REVENUECAT_PUBLIC_KEY_ANDROID` (`goog_…`), optionally `REVENUECAT_SECRET_KEY` (REST v1 secret, for the sync fallback), `STORE_TERMS_URL`, `STORE_PRIVACY_URL`; (3) the store subscription: Google Play products belong to one package name, so the preview package needs its own Play listing on the internal testing track with the subscription `club_membership_annual` (annual base plan), or the flow is first checked with RevenueCat's Test Store keys (`test_…`, no store listing) and the real subscription is created for the production package before launch; the App Store Connect subscription and `REVENUECAT_PUBLIC_KEY_IOS` follow with the iOS build; (4) then ONE `eas build --platform android --profile preview` (owner's OK; the OTA channel keeps working for JS-only changes afterwards) and on the device: sign in → «العضوية» → «اشترك الآن», and `POST /api/admin/push/test` with an admin session to see a push arrive. Still open from session A: Paymob support outcome, live hub key, Postgres, gap 5 wording, WhatsApp handover on a real device; then the dashboard and the iOS build.
- [ ] **Golden offer pages in-app** (owner's priority, 2026-09-13): a native page per golden company mirroring its web `/offer/` page (see §5.1); content source to decide at the start of that session; then no in-app link to the golden companies leaves the app.
- [x] **M9 — «رسائل الإدارة» and the dashboard's first section** (owner priority 1, 2026-09-16, §5.5): admin posts with text, links, images and video from a web dashboard (`admin/`, hub admin login), the posts block at the top of the home, push per post. CLOSED 2026-09-23: everything has been live since 2026-09-20/21 (dashboard.vibesholdingsa.com, live hub key, bucket uploads, the owner's real post with an image, the block first on the home) — see the status lines below. Still open ONLY: push delivery needs the FCM V1 key on expo.dev (and the first key pasted into chat must be REVOKED) — carried in the gaps list, not by this box.
  - Status (2026-09-20, branch `m9-admin-posts` from `m7-payments`, local commits 3d58af6 to 9dea64f, not pushed): SERVER, DASHBOARD AND APP CODE DONE AND CHECKED LOCALLY; NOT DEPLOYED (no push, no Railway `admin` service, no domain, no OTA), so the box stays open. `server/src/posts/` (kv `admin:posts`; media are links only: images by URL, video as a YouTube id; uploads need a file store, owner has not decided); `GET /api/posts`, `GET /api/posts/:id`; admin `GET/POST /api/admin/posts`, `PUT/DELETE /api/admin/posts/:id`, `POST /api/admin/posts/:id/notify` → new `PushService.broadcast` with data `{ type: 'post', postId, screen: '/posts/<id>' }`. `npm test` 68 pass (7 new in `src/m9.test.ts`, mock hub + MemoryKV + stubbed Expo push; Postgres and real push delivery not exercised). Dashboard origins: `ADMIN_ORIGINS` (comma separated, documented in `server/.env.example`), a request hook in `app.ts` answers preflights and sets `access-control-allow-origin` only for listed origins, no new package. `admin/` = Vite 8 + React 19 + TypeScript (`api.ts` typed client, Bearer token in localStorage; `Login.tsx`: body is `{ login, password }`, and the hub may answer `pending: true`, then the page asks for the 6-digit code and calls `/api/auth/verify`; `App.tsx` posts list, delete, notify; `PostEditor.tsx`); API URL from `VITE_API_URL`, default = the test API. Verified 2026-09-20: `npm test` 69 pass; `admin` typecheck and `npm run build` pass; against a locally running server over real HTTP: allowed origin preflight 204, unlisted origin gets no CORS header, register → verify → login → create published post → public feed, and an Arabic title sent with Node fetch is stored identically. Browser check (owner's OK, 2026-09-20, local server + mock hub): session restore, posts list, create and publish (stored fields equal the form), edit (pin; `publishedAt` unchanged), delete, 375px layout without horizontal overflow, IBM Plex Sans Arabic loaded, RTL. Found and fixed there: with no registered device the dashboard said the notification was sent; now `POST …/notify` answers 409 `no_devices`, a post is marked notified only when `sent > 0`, and the button is disabled with an explanation when `devices` is 0 (`npm test` 71 pass). NOT verified: the login form through the UI (no password was typed into it; its API contract is verified over HTTP), unpublish and logout buttons, the success path of notify with real devices, real push delivery, Postgres. Tooling: `.claude/launch.json` (dashboard dev server for the browser pane) and `admin/.env.development.local` are local files, not committed. Gotcha: inline Arabic in a Git Bash `curl -d` is stored as `?` marks (not a server bug), and in-batch browser screenshots can be stale: read the DOM. Remaining: a static server/start script for the `admin` Railway service, the app's home block + `/posts/[id]` screen + push tap handling (JS only), then the owner's steps (new Railway service with root directory `admin`, `ADMIN_ORIGINS` on the API service, custom domain + CNAME at Hostinger).
  - Checked on 2026-09-17/20: live `/health` says `storage: memory`; in code that happens only when `DATABASE_URL` is empty (a failed connection would crash boot), so the variable is empty on Railway; whether the Postgres service exists there is unknown. `origin/main` is 5 commits behind `m7-payments`, so the live server has no `/api/membership/store` and no RevenueCat webhook route yet. Firebase: owner created project `vibes-holding` with the Android app `com.vcmem.investorsclub.preview`; `app/google-services.json` is in place (git-ignored), `GOOGLE_SERVICES_JSON` is set in `app/.env` and as an EAS preview file variable; the FCM V1 service-account key is still to be uploaded by the owner on expo.dev credentials.
- [x] **M10 — Neutral first** (§5.5): persona order محايد → رائد أعمال → مستثمر everywhere, the «دليل المحايد» page, in-app workshops page with registration, neutral benefits wording.
  - The order and «فئة» wording were done across app/server/plugins in the 2026-09-21/22 sessions. The rest DONE 2026-09-23 (this session; server + dashboard + app JS, one OTA): new content block `content:guide` («دليل المحايد والورش», `server/src/content/guide.ts` + `en/guide.ts`, seed v1) — title, intro, 4 steps with in-app CTAs (advisor / workshops / projects / membership), neutral benefits list, and the workshops schedule (3 seeded items, free-text `schedule`, `mode: hq|online|both`, `open` flag) — all editable from «محتوى التطبيق» and «القيم والأسعار» like every block; `GET /api/guide?lang`. The home's neutral portal now targets `guide` (HOME_SEED v4; the app maps unknown targets to the advisor, so older JS is safe). Workshops registration `server/src/workshops/` — `POST /api/workshops/register` for ANY signed-in account (no membership: the neutrals are the audience; guests meet the sign-in words), one registration per workshop per account, notifier mail «تسجيل في ورشة», `GET /api/workshops/mine`, admin `GET /api/admin/workshops`; dashboard section «تسجيلات الورش» (counts per workshop + the rows). App: `guide.tsx` (numbered step cards + benefits, server-driven words), `workshops.tsx` (schedule cards, mode pill, inline note + «تأكيد التسجيل», «سجّلنا اهتمامك» state). Tests `m10.guide.test.ts` (4); browser + HTTP flow verified locally (registration → mail → dashboard row; guide in both languages). i18n groups `guide` (+ `people`), catalog 714.
- [x] **M11 — «شخصية ومسيرة»** (§5.5): home section with the owner's line, self-application form, admin approval and editing in the dashboard, public profile pages.
  - DONE (2026-09-23, this session; server + dashboard + app JS, one OTA). Server `server/src/profiles/` (KV `profiles:items` + `profiles:config`): a member with an ACTIVE membership applies (title, company, bio, milestones, links; his name from the account) → pending + notifier mail «طلب جديد في شخصية ومسيرة»; the admin approves (push «تم اعتماد ملفك» → `/people/<id>`), rejects with a note the member reads, edits any profile, adds profiles by hand (public at once), orders them (`order`, smaller first) and edits the owner's intro line (seed: «لديك الخبرات والمهارات والمؤهلات والإمكانات التي تؤهلك لتكون إحدى شخصيات نادي المستثمرين.» — he completes/edits it from the dashboard); an EDIT of an approved profile waits in `draft` while the approved words stay public until approved. Public `GET /api/people` (+`/:id`) — approved only; profile changes bump sync `content` so open apps refresh in seconds. The PHOTO is dashboard-only for now (uploads module; the current binary has no image picker — self-serve photo needs the next native build, noted for M19/M27 stage 4) — the app form says so and points at «راسل الإدارة». Dashboard section «شخصية ومسيرة» (pending first, editor sheet with photo upload + draft review, manual add, intro editor). App: home strip `PeopleBlock` right after the portals (renders nothing while empty), `people/index`, `people/[id]` (photo/initial avatar, bio, gold-dot milestones, tappable links), `people/apply` (status-aware form; guests → sign-in; no membership → the membership gate), account-tab row. Tests `m11.profiles.test.ts` (6); 225 server tests pass; dashboard checked in the browser (list, editor, intro save at phone width), full HTTP flow 18/18 locally. Decisions (one line each to change): active membership required to apply; resubmission model = draft-behind-approved.
- [ ] ~~**M12 — Members' chat**~~ cancelled by the owner on 2026-09-16; nothing to build.
  - App part (2026-09-20, JS only, no new native module): `app/src/api/posts.ts`, `components/PostsBlock.tsx` (+ `PostCard`), `src/app/posts/index.tsx`, `src/app/posts/[id].tsx`, both registered in `src/app/_layout.tsx` with `detailHeader`; the block sits right under the hero, above the portals (my reading of the owner's «أول بلوك»: confirm; moving it above the hero is one line) and renders nothing when there are no posts; its title «رسائل الإدارة» is a constant in the app, not server content. `PushRegistrar` already routes a tap to `data.screen`, so `/posts/<id>` needs no change there; a `post` notification now invalidates the `posts` cache. Devices register for push only after sign-in, so a post notification reaches signed-in members only. Verified on the local emulator (AVD `Medium_Phone_API_36.1`, the 2026-09-13 debug APK installed over an older one that the snapshot restores, Metro on 8081, local server through `10.0.2.2`): home block with pinned first and the draft hidden, post screen (badge, time, text, YouTube thumbnail, image, link button), deep links `investorsclub-preview://posts/<id>` cold and warm; server test pages through posts with the app's cursor rule, each post exactly once (`npm test` 72 pass). Two one-off observations, cause NOT determined, not reproduced: (1) the very first post deep link of the session left the app on the home while `://videos` worked; fine after a Metro restart with `--clear` (module count unchanged, so a stale route cache is not proven); (2) once, about 5 s after a cold start that coincided with a hot JS update, the app itself opened the in-app browser on the post's link URL with no tap (logcat: `BrowserProxyActivity` from the app's uid); 0 occurrences in 4 later cold and warm runs. Watch for it on a real device. NOT verified: the posts list screen and «عرض الكل» on the emulator, a real push tap, the OTA/export bundle.
  - Dashboard start script (2026-09-20, commit adc15f7): `admin/server.mjs` (no dependencies: serves `dist/`, single-page-app fallback, a year of caching for `/assets/`, gzip, CSP with `script-src 'self'`, no framing, HSTS; paths never leave `dist/`) and `npm start`; Railway runs `npm run build` then `npm start`. Checked locally over HTTP (headers, fallback, 404 for a missing asset, traversal attempts, 405) and in the browser (the login page renders under the CSP with the font, RTL, no console errors); not yet run on Railway. File store decision (the owner delegated it, 2026-09-20): a Railway Bucket (private, S3-compatible, about $0.015 per GB-month, bucket egress carries no fee, same Railway account, no new subscription); R2 was dropped because a public R2 bucket needs the domain's DNS on Cloudflare, and a Railway volume because it ties files to one service and costs ten times more. Live `/health` on 2026-09-20: still `storage: memory` (posts would vanish on every deploy: attach Postgres before real use), hub mock, mail configured (4 recipients), Paymob test keys in live mode, no RevenueCat keys, 0 push tokens. This PC: no Railway CLI, no Chrome extension connected. Railway, seen in the owner's session on 2026-09-20 (project faithful-nurturing, environment production): the account is still on the TRIAL (22 days or $4.41 left; the owner must choose the Hobby plan himself or every service stops; the trial allows one custom domain); the Postgres service exists and is online, but `DATABASE_URL` on the API service is an EMPTY STRING, which is why `/health` says `storage: memory`. Done there by Claude: a new empty service (temporary name `thorough-cooperation`, to rename `admin`) with root directory `/admin` and the custom domain `dashboard.vibesholdingsa.com` (target port 8080; Railway waits for DNS), changes staged, not deployed. BLOCKED for Claude by the desktop app's safety classifier, whatever the owner says in chat: writing Railway variables, editing DNS records, and the GitHub pop-up of «Connect Repo». So the owner does by hand: (1) Hostinger DNS of vibesholdingsa.com: CNAME `dashboard` -> `3vb8j2rq.up.railway.app` and TXT `_railway-verify.dashboard` with the `railway-verify=...` value from the service's «Show DNS records»; (2) the new service: Settings -> Connect Repo -> `Yasserpsd/Vibes-Holding-APP`, then Deploy; (3) API service variables: `DATABASE_URL` = `${{Postgres.DATABASE_URL}}`, new `ADMIN_ORIGINS` = `https://dashboard.vibesholdingsa.com`, then Deploy. If the dashboard answers 502 after the deploy, the service listens on Railway's `PORT`: compare it with the domain's target port 8080.
  - DEPLOYED (2026-09-20, later the same day; the owner did the blocked steps by hand, each one verified from outside): the branch is pushed and merged (`origin/main` = ef97a4d), live `/health` says `storage: postgres` and `uploads: { mode: 'disk', durable: false }` (M15 server live, no bucket yet), the CORS preflight from `https://dashboard.vibesholdingsa.com` answers 204 with the origin allowed (`ADMIN_ORIGINS` set), the CNAME and the `_railway-verify` TXT resolve, the Railway service `thorough-cooperation` (still to rename `admin`) is connected to `Yasserpsd/Vibes-Holding-APP` branch `main` with root directory `/admin`, and https://dashboard.vibesholdingsa.com answers 200 with the security headers, the single-page fallback and the M15 build; the login page renders there with the font and no console errors. NOT verified live: signing in through the form and publishing a post. Open risk: the hub is still the MOCK on this public server, where any e-mail with `+member@` becomes an admin with code 123456, so anyone who finds the dashboard URL could post to the testers' app; the fix is the live hub key (`HUB_SITE_KEY`, `HUB_MODE=live`). Still open: the Railway plan (trial), the Bucket and its five `S3_*` variables plus `UPLOADS_SWEEP=1`, the FCM V1 key on expo.dev (the first key was pasted into the chat by mistake and must be revoked), `eas update` for the M15 app tile.
  - HUB LIVE (2026-09-20, evening; the owner created the tenant site in the hub «المواقع» and set `HUB_SITE_KEY` on Railway himself): live `/health` says `hub: { mode: 'live', registrationOpen: false, adminOnly: true, keyFingerprint: '40:1dd12466' }`, so the mock hole (code 123456, every account an admin) is closed on the public server; a wrong login answers 401 `bad_login`. Incident on the way: the owner's `HUB_MODE` value was not exactly `live`, the new deployment crashed at boot (`Invalid environment: HUB_MODE: Invalid option`) and the API answered 502 for roughly half an hour although `server/railway.json` has a healthcheck (why the old deployment did not stay up was NOT determined; a manual restart of it with the new variables is one possibility). Fix: the owner REMOVED `HUB_MODE`; the server goes live by itself when `HUB_SITE_KEY` is set (`config.ts`), so do not re-add it. Lesson: hand the owner paste-only values and prefer designs where a variable can be left out. NOT verified: a real sign-in with a hub admin account through the dashboard and the app (the owner's step). Still open: the Bucket and its `S3_*` variables (references confirmed in Railway's docs: `BUCKET`, `ENDPOINT`, `REGION`, `ACCESS_KEY_ID`, `SECRET_ACCESS_KEY`; a literal unresolved `${{...}}` in `S3_ENDPOINT` would crash the boot, it must be a URL), the Railway plan, the FCM key, `eas update`.
  - BUCKET LIVE (2026-09-20, evening; the owner created the Railway Bucket, display name `ample-vault`, and pasted the five `S3_*` references `${{ample-vault.*}}` plus `UPLOADS_SWEEP=1` on the API service himself): live `/health` says `uploads: { mode: 's3', durable: true, cors: 'ok', endpoint: 't3.storageapi.dev', sweep: true }`. Verified the same evening: the owner signed in to the live dashboard with his hub admin account and published a real post with one image picked from his device (so the live hub login, the presigned POST from a browser and the bucket CORS all work); the public feed carries its `/media/posts/2026/09/<uuid>.jpg` link, which answers 302 (`Cache-Control: public, max-age=600`) to a presigned `ample-vault-….t3.storageapi.dev` link that serves 200 `image/jpeg`. NOT verified yet: a video upload and its `/media` 302 with ranges (the owner's post had no video), the image inside the app (expo-image following the 302), the app tile after `eas update --channel preview`.
- [x] **M13 — AI project summary in the Projects Bank** (owner, 2026-09-20: «AI يلخص المشروع بالكامل ويعمله في شكل جدول شيك جدًا ويظبط الكلام المكتوب كله»): an option on the project page that shows the whole project as an elegant table with the wording polished. CLOSED 2026-09-23: shipped inside bridge v2 (2026-09-21, live) as the project page's «ProjectBrief» table (`server/src/projectsBank/brief.ts`, OpenAI + rules, cached, both languages, rule 4 enforced, «ملخص آلي» disclaimer) — M35 then put it FIRST on the page. Constraints: rule 4, the summary is built from public fields only and never carries founder contact data or the pitch deck (founders' free text can embed phones, e-mails and links: strip them); no invented numbers, no promised returns, a «ملخص آلي» disclaimer. To decide with the owner at the start of that session: provider and cost, generate once and cache or on demand, who sees it and whether it consumes «رصيد».
- [x] **M14 — The advisor everywhere** (owner, 2026-09-20: «المستشار داخل في كل شيء في التطبيق… المساعد اللي يساعد الشخص في كل شيء»): an advisor entry on every screen that opens the conversation with that screen's context (extends §5 item 5). CLOSED 2026-09-23: shipped inside bridge v2 (live) — the floating «اسأل المستشار» button + context registry (`AskAdvisor`, `useAdvisorScreen`) on the screens, with the screen's context sent as focus; the news rule (7) holds (the advisor discusses items, never writes them) and the «رصيد» question was settled by direction (8) (hidden until <10% left). To confirm with the owner: on news screens the advisor may discuss an item in the chat, it never writes or rewrites feed items (rule 7); effect on «رصيد» consumption.
- [x] **M15 — Uploads in the dashboard** (owner, 2026-09-20: «ارفع صورة من الجهاز أو الجوال… صور وفيديو… كأني بنشر على واتساب»): pick images and video from the computer or phone in the post editor instead of pasting links. CLOSED 2026-09-23: live since 2026-09-20 evening — Railway Bucket `ample-vault`, `/health` → `uploads: s3, durable, cors ok`, the owner published a real post with an uploaded image from his device and the app serves it through the `/media` 302. Still unverified live (watch when it first happens): a video upload end to end. Needs a file store, which is a paid-service decision for the owner (options to present: Cloudflare R2, a Railway volume); video needs size limits and a playback path in the app.
  - Status (2026-09-20, branch `m9-admin-posts`, commits 24a8f5b server, 1143040 dashboard, 62f2d70 app; NOT pushed, NOT deployed, no bucket yet, so the box stays open). Server `server/src/media/`: `POST /api/admin/uploads` (admin) answers an upload ticket: with a bucket a presigned POST whose policy pins key, content type and size (the browser sends the file straight to the bucket), without one a signed `PUT /api/admin/uploads/put?ticket=` to the local disk store `UPLOADS_DIR` (development and tests only, `durable: false`, refused in production); `GET /api/admin/uploads/config`; public `GET /media/<key>` = 302 to a one-hour presigned link (bucket) or the file with ranges (disk); keys are server-made `posts/YYYY/MM/<uuid>.<ext>`; images JPG/PNG/WebP/GIF up to `UPLOAD_MAX_IMAGE_MB` (10), video MP4/MOV/WebM up to `UPLOAD_MAX_VIDEO_MB` (200), QuickTime is stored and served as MP4 (Android's browser refuses the QuickTime label), SVG/HTML refused. Posts: input `videoFile { url, poster }`, output `video { url, poster } | null` (older posts have no key), `images` unchanged (uploaded images are `/media/` URLs next to pasted links); saving checks our media with HEAD (`invalid_media`), files a post stops using are removed unless another post uses them, an opt-in daily sweep (`UPLOADS_SWEEP=1` and Postgres) removes day-old abandoned uploads and never runs when no post references any file; the server writes the bucket CORS rules from `ADMIN_ORIGINS` at boot only on Railway (`/health` -> `uploads.cors`). Variables in `server/.env.example`: `S3_BUCKET`, `S3_ENDPOINT`, `S3_REGION`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY` (set them by reference to the Railway Bucket), `S3_FORCE_PATH_STYLE`, `UPLOADS_DIR`, `UPLOAD_MAX_*`, `UPLOADS_SWEEP`. New packages: `@aws-sdk/client-s3`, `@aws-sdk/s3-presigned-post`, `@aws-sdk/s3-request-presigner`. Dashboard: «إضافة صور» / «إضافة فيديو» pickers, progress with cancel, thumbnail grid with reorder and remove, poster frame captured in the browser, limits checked before sending, warning when files are not durable. App (JS only, OTA-safe): `post.video` tile on the post screen and the play mark / poster on the cards, opened through `openLink` (in-app browser). Verified: `npm test` 85 pass (13 in `src/m15.test.ts`: tickets, disk store, ranges, traversal, posts with media, clean-up, sweep, a presigned bucket ticket built offline), server / admin / app typecheck, admin build, app lint; in the browser against a local server (mock hub, disk store): two images and a WebM clip uploaded from the editor at 375px, SVG refused in Arabic, poster captured, publish, the public feed carries `video`, re-edit right after save keeps the files, two batches keep their picked order. A multi-agent review found and the code fixes: an unauthenticated large-body path on the PUT route, a sweep that trusted one server's view of the posts, deletion inside the size check, a stale list in the dashboard that could delete fresh uploads, interleaved image order. NOT verified: anything against a real bucket (presigned POST from a browser, CORS, the 302 for expo-image and for video in the in-app browser), the app tile on the emulator or a device, HEVC `.mov` from an iPhone on Android. Known leftovers: `npm audit` flags `nodemailer` (older finding, not from this change); reorder buttons lose keyboard focus after a move. To deploy: owner pushes; in Railway: Create -> Bucket, then on the API service the five `S3_*` variables by reference to the bucket plus `UPLOADS_SWEEP=1` (variable writes are the owner's, see the M9 status), check `/health` -> `uploads: { mode: 's3', cors: 'ok' }`, upload from the live dashboard, then `eas update --channel preview` for the app tile.
- [ ] **M16 — In-app forms and notification recipients** (owner, 2026-09-20): a native in-app copy of every web form (شركاء النجاح, workshop registration, any other form; list them from §5.2 first) and a dashboard setting for who receives the e-mail when someone registers or pays (today one `NOTIFY_EMAIL` variable on Railway).
- [ ] **M17 — Visual polish** (owner, 2026-09-20: «خلي شكل المشاريع أشيك… وطريقة عرض المحتوى أشيك»): redesign of the Projects Bank cards and project page and of content presentation in general; agree on a direction with the owner before building.
- [ ] **M18 — Dashboard sign-in code by e-mail** (owner, 2026-09-20: «باسوورد يجي على إيميل يكون متاح لمدة دقيقة واحدة فقط وبعد كده يصبح غير صالح»): a one-time code e-mailed to the admin when signing in to the dashboard, valid 60 seconds (make it a variable, e.g. `ADMIN_OTP_SECONDS`), single use. Constraints: rule 2, the identity stays the hub's (the code is a step on top of the hub account, never a separate admin registry); store only a hash of the code, limit attempts and resends, never log the code; the app's member sign-in is not affected. Risk to tell the owner: mail delivery can take longer than a minute (measure the SMTP delay first, offer «إعادة الإرسال»). To confirm at the start: a second step after the hub password, or a code instead of the password. Owner's decision (2026-09-20): a second step after the hub password.
  - Status (2026-09-20, branch `m9-admin-posts`, NOT pushed, NOT deployed, `ADMIN_OTP` stays 0 until the mail delay is measured, so the box stays open). Server: `server/src/auth/adminOtp.ts` (challenge store: code hashed with the challenge token, 60 s life from `ADMIN_OTP_SECONDS`, single use, 5 wrong codes end the challenge, first mail + 3 resends with a 20 s gap, challenge lives 10 min; the 60 s start when the mail has left, and a resend whose mail fails changes nothing; per hub account, whatever the address: one live challenge, 6 challenges and 10 wrong codes an hour, then `otp_cooldown`; steps run one at a time per challenge and per account because the KV has no atomic counter); `AuthService.adminLogin / adminVerify / adminResend`; routes `POST /api/admin/auth/login | verify | resend` (per-address limits). The hub still checks the password and the admin role (rule 2), the role is read again at verify; the session record gets `adminVerifiedAt`, and `dashboardGuard` (posts, uploads, push, payments, membership purchases) asks for it while `ADMIN_OTP=1` and for a new code after `ADMIN_SESSION_HOURS` (12); a lapsed dashboard session is revoked on its first refused request. `/api/admin/hq/*` keeps `adminGuard`: the app's reception screens and member sign-in are untouched. The code is only in the mail body (never subject, log, response or storage). No SMTP on the live hub = the dashboard refuses to sign in (`otp_mail_off`, fail closed); way back in: set `ADMIN_OTP=0` on Railway. `/health` -> `hub.adminOtp`: `off | on | no_mail`. On the mock hub without SMTP the code is the mock's public test code (local runs). Dashboard: `Login.tsx` code step with a seconds countdown, «إعادة الإرسال» and «رجوع»; it signs in through `/api/admin/auth/login` in both modes and falls back to `/api/auth/login` only when the server answers 404 `not_found` (a server from before M18), so the dashboard and the API can deploy in any order; after the hub's own «pending» e-mail code it drops that member session and signs in again through the dashboard route. Verified: `npm test` 101 pass (16 in `src/m18.test.ts`), server and admin typecheck, admin build, and in the browser against a local server (mock hub, `ADMIN_OTP=1`): code step, wrong code, used challenge, resend, sign-in, posts load. A multi-agent review (12 agents) confirmed 8 findings; fixed: no per-account limit, resend state changed before the mail left, the code clock started before the send, lapsed sessions never revoked, the «pending» path ending in a refused session, no fallback for an older server, the lock-out note in `.env.example`. Known leftovers: a post editor open across the 12 h cut-off loses its unsaved draft on save (wanted: ask for the code in place without unmounting the editor, `App.tsx` + `PostEditor.tsx`); the per-address limits trust `request.ip` with `trustProxy: true` (check how Railway sets `X-Forwarded-For` before relying on them; the per-account budget does not depend on it); `/health` says `on` when SMTP is configured, not when mail really arrives. NOT verified: a real mail through the hosting SMTP and its delay. To turn on: owner pushes; on Railway set `ADMIN_OTP=1` (variable writes are the owner's), check `/health` -> `hub.adminOtp: 'on'`, sign in once and time the mail; if it takes longer than a minute raise `ADMIN_OTP_SECONDS` (e.g. 180). An unverified hub e-mail still goes through the hub's own code first, then signs in again for the dashboard code. M25 (publisher role) must reuse `dashboardGuard`.
- [ ] **M19 — Video plays inside the app** (owner, 2026-09-20: «أي فيديو يتم فتحه داخل التطبيق… يوتيوب أو أنا رفعت فيديو… مش محتاج تطلع برا الأبلكيشن أبدًا»): a player screen inside the app for YouTube videos (the videos section and a post's `youtubeId`) and for a post's uploaded video, instead of `openLink`. Facts (2026-09-20): the app has neither `expo-video` nor `react-native-webview`, only `expo-web-browser`, so this needs native modules and a NEW preview build (quota: the owner's explicit OK), not an OTA; YouTube must play in YouTube's own embedded player (its terms), uploaded files in `expo-video`; gate both with `requireOptionalNativeModule` so the old binary keeps the in-app browser fallback (see the OTA crash lesson). Also list every other place that leaves the app and decide each with the owner («اقرأ من المصدر» stays the in-app browser, rule 7; Paymob checkout).
- **Full dashboard** (owner, 2026-09-20: «عايزه داشبورد مميز جدا فيه تفاصيل يومية لكل شئ… اعمل كل شئ تقريبا في ابلكيشن»). Split into M20–M24, one per session. Facts (2026-09-20): the dashboard has only sign-in and the posts editor (`admin/src`: `Login.tsx`, `PostEditor.tsx`, `App.tsx`); the hub bridge ops are `ping, config, register, resend_code, verify, login, logout, account, profile, reset_request, reset_confirm, delete_account, activate_member, message, poll, history`, so there is NO op to list or count members and none to change a member's Projects Bank unlocks: M20 and M21 need new admin ops in the hub plugin (and M21 one in `projects-directory-pro`), which the owner uploads to the live sites (his OK each time). Rule 2 holds: the server asks the hub and caches, it keeps no member registry. Do M18 before M21 (the dashboard gains the power to upgrade members).
- [x] **M20 — Dashboard home: members and payments numbers** (owner: «عدد الأعضاء المسجلين في المنظومة كام واحد دفع وكام واحد لسه مدفعش… تفاصيل يومية»): registered / paid / not paid yet, new sign-ups and activations per day, app service payments per day (Paymob, from our own table), expiring memberships. Needs a hub admin op for counts and a daily series (e.g. `admin_stats`), called with the bridge key, cached a few minutes.
  - LIVE since 2026-09-21 (test environment): the owner merged PR #6 and PR #7 into `main` (Railway deployed the server and the dashboard), uploaded hub 2.7.0 to vcmem.com and Projects Bank 39.0 to vibesholding.com, set `HUB_WEBHOOK_SECRET` and `PB_BRIDGE_KEY`, and reported the n8n v4.0 import and the other sites done. Checked from outside: `/health` → `bridge.hub 2.7.0 ok`, `bridge.pb 39.0 ok`, `pbMode live`, `hubWebhook true`, `brief openai`; `/api/sync` versions move on hub and Projects Bank webhooks; `/api/feed` serves the sites' new pages; the live dashboard serves the new bundle; the debug app on the emulator against the live server drew an AI brief of a real project. OTA published to `preview` (runtime 1.0.0, update group `565f5f2e-5bc1-4913-b991-9e85831f0641`, commit `a7491a3`). Added the same day: the brief's `stage.estimated` (57 of the 260 live projects carry the stage «أخرى» or none; the model's reading of what already exists places them, the site's own stage always wins). Not seen by Claude: the dashboard with the owner's account (the «خادم التطبيق» box), a real n8n v4.0 conversation. M25 (server side) and M26 (sign-up review on the live sites) stay open.
  - Status «bridge v2» as built (2026-09-21, branch `m20-bridge-v2` on top of `m9-admin-posts`). One session built M20 + M21 + the plugin parts of M25 and M26 + the in-app adviser, by the owner's wish («قسم الشغل برحتك… العقل واحد»). The contract and the API as built: `docs/BRIDGE_V2.md` (read it before touching any part).
    - Finding that shaped it: the owner's hub plugin 2.6.0 had lost our 2.4.1 `delete_account` patch and never had `activate_member`, so store purchases could not activate a membership on the live hub. Both are in 2.7.0.
    - Deliverables on the owner's drive in one folder he asked for, `D:…Ai AgentNew Era` (his originals stay in `…Ai Agentibes-ai-assistant`): `خطوات-التشغيل-2.7.0.md` (his go-live steps, in order), `1- بلجن المساعد 2.7.0 …ibes-ai-assistant-2.7.0.zip`, `2- بلجن بنك المشاريع 39.0 …projects-directory-pro.zip`, `3- عقل n8n v4.0Vibes_Web_Agent_v4.0.json` (holds his live n8n keys: never in git) with `n8n-v4.0-README.md` (the zip has no top folder, so WordPress names the plugin folder after the FILE name: it must stay `projects-directory-pro.zip`). Source diffs: `docs/hub-plugin/vibes-ai-assistant-2.7.0.diff`, `docs/hub-plugin/projects-directory-pro-39.0.diff`; n8n builder + prompt: `docs/hub-plugin/n8n/` (`node build-v4.mjs <v3.2 export> <output outside the repo> [--app-host=<host on the hub's sites page>]`, default `app.vcmem.com`).
    - Hub 2.7.0: trusted-site flag (`vai_sites.trusted`, checkbox «خادم التطبيق» on «المواقع») + admin actor check on every privileged op; `vai_events` history with a one-time bounded backfill, `contacts.verified_at`; `member_change()` (transaction, idempotent on `source:ref`, extend keeps the start date and adds days; `set_member()` and the Paymob path behave as in 2.6.0); ops `delete_account, activate_member, admin_stats, admin_accounts, admin_member, admin_payments, admin_tickets, admin_leads, admin_threads, admin_thread, admin_reply, admin_mail, admin_grant, admin_set_role (member | publisher), changes, publish, feed`; signed webhook to the app server; `[vai_feed]` shortcode on every site; `message` takes `context` + `page_text` (→ n8n `focus`, `page.live_text`); widget «المستشار» (ask-about-this chip, tables, new sign-up); a sign-in through the trusted site writes no «welcome back» line and spends no model call; quick replies up to 4; `totals.accounts = members_active + members_expired + unpaid`.
    - Projects Bank 39.0: `inc/bridge.php` (`ping, balance, balances, unlock, grant, unlocks`, header-only hashed key, `GET_LOCK` around count-and-insert, grants table with idempotent `ref`, every paid year of a membership renewed early gets its own allowance), `project.changed` webhook, «اسأل المستشار» on cards (public fields only).
    - n8n v4.0: Simple Memory unplugged (the hub's turns are the only history), prompt 44.8k → 9k characters, no forced pitch / greeting / closing question, content-level anti-repetition, up to 3 tools and 6 iterations, medium effort for analysis, focus + live page text first, app mode (no web price, no pay link; tables allowed in the app since its chat draws them), admins exempt from gates.
    - Server: `server/src/dashboard/*`, `server/src/sync/*`, `server/src/webhooks/*`, `server/src/projectsBank/bridge.ts` + `brief.ts`, `server/src/posts/hubSync.ts`, hub types/mock for every op; new env names in `server/.env.example`: `PB_BRIDGE_URL`, `PB_BRIDGE_KEY`, `HUB_WEBHOOK_SECRET`. Dashboard: `admin/src/sections/*` (home, members + member sheet, payments, tickets, leads, threads, mail, audit, posts with kind event). App (JS only, OTA-safe, no new package): `AskAdvisor` floating button + context registry, `ProjectBrief`, `ProjectUnlock`, `SyncPoller`, tables / headings / links in `RichText`, event posts.
    - Verified: `npm test` 144 pass, server / admin / app typecheck, admin build, app lint. A real local rig (scratchpad of the session, rebuildable: portable PHP 8.3.33 + MariaDB 11.4.13 + WordPress 7.1.1, hub on :8088, satellite + Projects Bank on :8089, our API in LIVE hub mode on :3009): 2.6.0 seeded with accounts and HMAC-signed Paymob callbacks, upgraded in place → 284 runtime checks pass (hub ops vs SQL truth, trust and actor gates, idempotency, Paymob answers identical to 2.6.0, webhook signatures, rule 4 on feed / brief / unlock, site → hub → app sync, dashboard writes with audit, n8n code nodes run on real hub payloads), `php -l` + a static call checker clean, both zips installed through WordPress's own `Plugin_Upgrader` («استبدال الحالية»), PHP debug logs empty. Dashboard checked in the browser at phone width against that rig (a Projects Bank grant made from the member sheet reached the plugin). App checked on the emulator (debug APK + Metro): home with the event post, floating button, project brief / stage / competitors, unlock 4 → 3 with the founder contact, adviser with the context pill and a drawn table, event page, new membership copy without the web price.
    - NOT verified / open: the live hosting's PHP version (needs ≥ 7.4; the owner checks Site Health); real n8n run of v4.0 (code nodes were executed outside n8n, the model was not called); «ناقش الخبر مع المستشار» on a device (news refresh was off in the rig); the widget's hover chip at desktop width in a real browser (the package ran headless checks only). Open decisions for the owner: Projects Bank still says 3,750 SAR for the five projects (gate benefits text = his PB settings; one hard-coded sentence in `inc/assistant.php`) while his new membership page says 2,500; Paymob renewals still restart the year from today as in 2.6.0 (only `extend` keeps remaining days): say if Paymob should extend too. Left for later sessions: M25 server side (role-aware `dashboardGuard`, publisher sees posts only; the hub role and `publish` permission exist), M26 sign-up flow review with the owner on the live sites, hub-side use of the widget's `client_id` / `retry_of` (a retry after an n8n failure still stores a second user row).
- [x] **M21 — Member management from the dashboard** (owner: «اقدر ارقي حد ازود رصيده في بنك المشاريع»): search members, member page (status, days left, unlocks left, payments), upgrade a member to the annual membership without a store purchase, add Projects Bank unlocks. Needs hub ops (`admin_members`, `admin_member`, an admin grant next to `activate_member`) and a Projects Bank server-to-server op for unlocks (§ line 29 already plans that plugin change). Every such action is written to an audit log (who, when, what, note) and asks for confirmation; nothing here is shown to members as a way to pay outside the app (rule 3).
- [x] **M22 — Usage analytics** (owner: «تفاصيل كاملة اكثر الصفحات زيارة»): the app reports screen views to our server (JS only, OTA-safe, batched, no third-party service), Postgres keeps daily counts per screen plus daily active users; dashboard shows most visited screens, per day / week / month. Aggregates only, no per-member browsing history unless the owner asks; mention it in the store data-safety forms (M8). DONE inside M33 (2026-09-23) — see M33 for where everything lives.
- [x] **M23 — Content control** («تحكم كامل في كل شئ من جوا داشبورد»): a dashboard section for every server-driven content block (benefits, prices, banners, golden companies, videos, services, about, HQ, news sources). CLOSED 2026-09-23: covered by «نصوص التطبيق» (every app string, M27 stage 1) + «محتوى التطبيق» (every block's texts in both languages, M27 stage 3; now 8 blocks with the guide) + «القيم والأسعار» (numbers, links, phones, booleans — M33) + «مصادر الأخبار» (M34). Still code-side ON PURPOSE (its own milestone if the owner asks): adding/removing rows (a new service, golden company, workshop…), enum/key/icon fields.
- [ ] **M24 — Live app preview with wording edits** (owner: «يكون ظاهر ابلكيشن نفسه قدامي محاكي كده واقدر اغير كلمة او حرف… لا يمسح ولا اي حاجه فقط تغيير كلمة»): the app inside a phone frame in the dashboard (Expo web export in an iframe; check first which native-only screens need a web fallback), an edit mode where clicking a text opens its wording; save = a server-side text override the app reads at start (OTA-free after the first release that ships the lookup). Edit only: no delete, no empty text, no layout change; the save refuses the banned wording from the UI and copy rules («مجاني», «كريديت», «الصفقات», hyphen between numbers). Needs every app string behind a key first: measure how many strings are hardcoded before promising a date. News items stay untouched (rule 7). First part built with M27 stage 1 (2026-09-22): the server-side overrides, the app's lookup and the dashboard section «نصوص التطبيق»; see M27.
- **Owner additions to the full dashboard** (2026-09-20, second message; he handed over the hub plugin: `D:\فايبز القابضة\فايبز القابضة\Ai Agent\vibes-ai-assistant\` = `vibes-ai-assistant.zip` (plugin 2.6.0), `دليل-التشغيل.md` (the version history and every hub admin page), `Vibes_Web_Agent_v3.2.json` (n8n workflow). Unpack the zip into the scratchpad and read it before writing any hub op (§2.1: never guess the plugin's code); package with `tar.exe -a`; the owner uploads it to vcmem.com (live system, his OK each time).
  - M20/M21 scope, his words: «اللي يدخل من موبايل ابلكيشن داشبورد يقدر يشوف نفس اللي موجود بالظبط في vcmem ويقدر يزود رصيد في بنك المشاريع… sync ما بينهم يكون اكبر بكتير… يظهر جنب كل عضو مفعل عضوية سنوية رصيد بنك المشاريع بتاعه… اللي يدخل عليها يحس انه داخل علي هيبة»: the dashboard mirrors the hub's admin pages (accounts with state, paid members with days left and daily AI رصيد, Projects Bank unlocks left beside every annual member, payments log, workshop tickets, leads and their seriousness grade, conversations, mail queue) through new bridge ops, works well on a phone, and gets a premium look. Admin only.
- [x] **M25 — «نص أدمن» (publisher role)** (owner: «متاح ليه فقط اني ارقيه للنشر فقط ميقدرش ولا يشوف ولا يعرف اي حاجه… اكسيس فقط النشر في ابلكيشن لا اكثر ولا اقل»): a second role next to admin. CLOSED 2026-09-23: superseded by M28 (2026-09-22, live) — the moderator role is exactly this (posts + uploads only, 403 everywhere else, «للجميع» only, the M18 code applies). Source of truth is the hub (rule 2): a «ترقية إلى ناشر» button on the hub's accounts page and a role field in the `account` answer (plugin change). Server: a role-aware dashboard guard; a publisher reaches only the posts and uploads endpoints (create, edit, publish), never members, numbers, payments, push tokens, HQ or settings; the dashboard shows him the posts section only. The M18 e-mail code applies to him too. In the app he stays an ordinary member. Decide with the owner: may a publisher delete or notify (push) a post, or only the admin.
- [ ] **M26 — Web assistant overhaul (the sites, not the app)** (owner, with two screenshots of the widget's sign-in and two-step register forms: «تجربة المستخدم… في التسجيل و ui و ux مش حلو خالص… احيان كتير بيكون مش فاهم المطلوب ولا بيدرس كويس السايت… مش بيظهر كده زرار في اي حاجه اقف عليها اساس المستشار… ويبقي اسمه المستشار»): rename the widget to «المستشار»; redesign registration and sign-in (agree on the flow first: fewer fields up front, clear errors, progress, phone field); a contextual «اسأل المستشار» button on the page's elements (cards, packages, projects) that opens the chat with that element as context; better page understanding (plugin sync + the n8n prompt). Plugin + n8n work, shipped by the owner to 13 live sites: plan it as its own sessions. Rule 3 is an app rule; the web keeps its Paymob links.
- Store subscriptions (owner, 2026-09-20, wants it done together in the browser): possible only with the owner signed in and approving each step; sign-in, agreements, payment and tax profiles are the owner's to do. Status of D-U-N-S, Play Console, Apple and RevenueCat accounts still unknown.
- Owner's directions of 2026-09-21 (after bridge v2 went live), done on branch `m20-bridge-v2` (commit `79d8fde`, needs his push + merge): (1) Projects Bank inside the app shows the project only: the founder's name is no longer sent by the API (`mapper.ts`), and `stripFunding()` in `server/src/projectsBank/redact.ts` cuts the funding sought, investment amounts, capital, valuations, equity on offer and investor-return talk out of founders' Arabic and English texts where the feed is mapped (checked on the 260 live projects: 56 of 520 texts change, descriptions stay whole; tests in `redact.test.ts`); the brief has no founder or «المطلوب» rows (cache version 3). The websites still show those facts: this rule is for the app. (2) Home portals in his wording and order: «محايد — أستكشف توجهي», «رائد أعمال — لدي مشروع», «مستثمر — أبحث عن فرص» (English lines stored as `titleEn` / `subtitleEn`); the same three lines are the persona labels at sign-up. (3) A neutral member is offered the club's forums, seminars and workshops, at the HQ in Riyadh or online from anywhere: the neutral opening, the adviser's portal context, and the n8n prompt (workflow **v4.1** in his `New Era- عقل n8n v4.0` folder, to import in place of v4.0; it also tells the adviser never to state a project's founder, funding or valuation inside the app).
- [ ] **M27 — English version of the whole app** (owner, 2026-09-21: «عايز ابلكيشن بالكامل يكون في من نسخة انجليزي ضروري جدا»). Measured the same day: 569 Arabic strings hard-coded in 57 files of `app/src` (about 12.7k characters), 281 strings in the 8 server content modules (about 11.9k characters, marketing copy the owner must approve in English), plus the server's user-facing messages and the hub's Arabic error texts (map them by `code` in the app). Hard fact: the binary forces RTL natively (`app.config.ts`: expo-localization `forcesRTL: true`), so a left-to-right English layout needs a NEW BUILD (quota, the owner's OK; worth sharing with M19's build); `expo-localization` is already in the binary, no new native module is needed. Steps: every string behind a key (this is also what M24 needs), a language setting (device language at first launch + a switch in «حسابي», direction applied with a reload), English server content per block (`?lang=` on the content routes; Projects Bank already has `titleEn`, `detailsEn`…), English news from English sources only (rule 7: never translated by AI; decide the sources with the owner), adviser and gate texts by language, store listing in English (M8). The dashboard stays Arabic. Decide with the owner first: the language switch, the English news sources, who approves the English marketing copy.
  - Stage 1 built (2026-09-22, branch `m27-english` on top of `m20-bridge-v2`, NOT pushed, NOT deployed, NO OTA yet). Owner's decisions: English news = Saudi news from foreign sources, plus Arab News and Saudi Gazette; nobody else reviews the English wording, he edits it himself from the dashboard (M24).
    - App (JS only, OTA-safe, no new package): `app/src/i18n/` = `strings/ar.json` (the reference) + `en.json` + `groups.json` (the dashboard's screen labels), `index.ts` (`t()`, `tOptional()`, `initI18n()` loads the language and the saved wording edits behind the splash screen), `direction.ts` (`isRTL`, `textStart`, `chevronForward`, `syncDirection()`, `switchLanguage()` = save + restart), `remote.ts` (`syncStrings()` at start and whenever `/api/sync` moves `content`), `lib/deviceStore.ts` (settings in expo-secure-store, long values in 600-character parts). Rule: call `t()` while rendering, never at module scope (the language is not loaded yet there). 108 keys so far: the tab bar, every stack title, the news screens and card, «حسابي» with the language row, StateView, PlaceholderScreen, PasswordPrompt, the API client's errors (`errors.code.*`: the English version never shows an Arabic server sentence), relative time and dates. `i18n/rtl.ts` is gone.
    - Direction, corrected fact: on the local debug build (same native code and the same `forcesRTL: true` strings as the preview APK) JavaScript turns the layout left-to-right with one restart, and it STAYS across cold starts; switching back to Arabic works the same way. So a new build may not be needed for English on Android. Not proven on the release APK yet: `UNPROVEN_RUNTIMES = ['1.0.0']` in `i18n/index.ts` keeps members on those binaries in Arabic, and only an admin sees the language row there (`languageChoiceOffered`). After the OTA the owner tries English on his phone: if the layout flips and stays after closing the app, empty that list in the next OTA; if not, the next build drops `forcesRTL` from `app.config.ts` (plugin option and `extra`, keep `supportsRTL: true`) and bumps `version`. A restart loop is impossible: one language gets one restart (`investorsclub.lang.restart`).
    - Server: `lang.ts` (`langOf`: `?lang=`, then the app's `x-app-lang` header; a browser's accept-language is never read); news per language (`/api/news/feed | decisions | topics`, labels by `tierLabel` / `topicLabel`); the English sources in the seed with `NEWS_SEED_VERSION = 2` and `mergeSeed()` (a stored list gets the new rows and that revision's changes once, the owner's edits stay); a `saudiOnly` source is collected only when its title or first 220 characters name Saudi Arabia (nothing else costs a page fetch or a classifier call); foreign outlets lead the English feed (`feedTierRank`). Checked from this PC on 2026-09-22, ON: SPA English wire (3 desks), CNBC (Bing), BBC Middle East, AGBI Saudi tag, The National business, Semafor (Claude's addition, because four of the owner's names cannot pass rule 7), Arab News, Saudi Gazette (2 feeds). Seeded OFF because their article pages refuse the server: Reuters 401, Bloomberg 403, Financial Times 403, AP 403. Found on the way and fixed for both languages: the SPA CDN caches by URL alone while the API picks the wire from `accept-language`, so two languages on one URL answered each other's rows; every SPA URL now ends with `&locale=` (the v2 merge rewrites the five stored Arabic URLs) and `parseSpaList` drops rows of the other locale. «United Kingdom» no longer counts as «the Kingdom».
    - M24, first part shipped with it: `server/src/appStrings/` (`catalog.ts` is GENERATED from the app's JSON by `npm run strings` inside `server/`, and `npm test` fails while it is stale; `GET /api/strings` for the app, `GET | PUT /api/admin/strings` behind `dashboardGuard`; a save refuses an empty text, a lost `{placeholder}`, «مجاني / كريديت / الصفقات», a hyphen between two numbers, and «free» in English; `value: null` = back to the app's own text; every save moves `/api/sync` `content`, so open apps read it within seconds) and the dashboard section «نصوص التطبيق» (`admin/src/sections/Wording.tsx`: version chips with the number of edits, screen chips, search, «المعدَّل فقط», inline edit with the other language shown under it). A screen appears in the editor as soon as its strings move behind keys. Still open in M24: the live phone-frame preview.
    - Verified: server `npm test` 160 pass; server, app and admin typecheck; app lint; admin build. Real feeds through a local server (`C:/Projects/vibes-test-rig/run-local-api-news.mjs`, launch entry `api-local-news`): the 21 enabled feeds answer, the English feed is English and about Saudi Arabia, the Arabic feed has no Latin title. Dashboard in the browser: the list, a refused «مجانية», a saved wording read back from `/api/strings`. App on the emulator (debug APK + Metro + that server): first start followed the device language (English, left-to-right), «حسابي» → the language row → Arabic (restart, right-to-left) → English again, cold start keeps it, English news tab with real items. NOT verified: the release APK (see the direction note), iOS, Railway's own address against the new feeds (after the deploy read `/health` → `news.sources` and turn off what fails there).
    - To go live (the owner): push `m27-english`, merge, Railway deploys server and dashboard; then the OTA (`eas update --channel preview`), which he runs or approves. Next sessions (3 is DONE, see stage 3 below): (3) server content per language (`langOf` on the 8 content modules, English edited by the owner from the dashboard), the adviser's starters, openings and gate texts (`app/src/components/advisor/context.ts`, still Arabic), hub error codes; (4) the English store listing (M8), and the build only if the release APK refuses the direction change.
  - Stage 2 built (2026-09-22, same branch, three commits `14a9638`, `2c2038b`, `b81ec8d`; NOT pushed, NO OTA). Every screen of the app reads its wording from keys now: 486 keys in 37 screen groups (`groups.json` labels them for the dashboard editor), all with English; the server catalog is regenerated (`npm run strings`, 162 tests pass); app typecheck and lint clean. Still Arabic in the app by design: the server content (stage 3) and the adviser's quick replies and openings in `context.ts` (stage 3). Personas and countries got app-side keys (`persona.*`, `country.*`) instead of a server `labelEn`, so the owner edits them from the dashboard like any other text; the hub's Arabic sentences go through `hubText()` (shown as they are in Arabic, the app's own key in English) and `errors.code.*` covers the auth codes (`bad_login`, `exists`, `phone`, `email_domain`, `bad_code`, `session_expired`, …).
    - Sign-in gate (direction (7) below): `app/src/app/welcome.tsx` + `app/src/auth/GuestGate.tsx`. The app opens on the gate (sign in, create account, «المتابعة كضيف»); `GuestGateProvider` keeps one guest minute per launch (`GUEST_SESSION_MS`, never saved; a timer plus an AppState check for a backgrounded app), then `locked` turns true and `<GuestGate />` in `_layout.tsx` sends the visitor back to `/welcome` («انتهت جولة الضيف»: sign-in and register only; the test build keeps the guest button because only admins can sign in there, rule 9). A member with a saved session never sees the gate: `AuthProvider.hasSession` covers the screen until the secure store has answered. The language link sits on the gate too. Verified on the emulator (Arabic and English): gate → guest → home → back to the gate after 60 s → sign in from the gate → home with the member's name.
    - Alignment, the owner's «حاجات مش مظبوطة… مشكلة في محاذاة», two causes found and fixed: (a) a Text reads `textAlign: 'left' | 'right'` RELATIVE to the layout direction on RN 0.86, so every hard-coded `'right'` drew Arabic titles, labels and last lines on the LEFT; `textStart` is now the constant `'left'` (= start edge) in both languages, `textEnd` = `'right'`, and only a TextInput uses `inputStart()` (physical sides). (b) `I18nManager.isRTL` is a start-up constant and reads `true` at every cold start of this `forcesRTL` binary, English included, so chevrons, arrows, inputs and the native header's back arrow followed the Arabic direction on English screens: `isRTL()`, `chevronForward()`, `arrowForward()`, `inputStart()` in `i18n/direction.ts` are functions read while rendering, and the Stack sits inside expo-router's `<LocaleProvider direction>`; the 130 direction-specific style spots are gone. Also fixed: «(.sa)» drew as «(sa.)» (left-to-right mark before the dot), and the membership card's long server title pushed its badge out of the card (`flexShrink`).
    - NOT verified: the release APK and iOS (the direction is applied by `forceRTL()` before the first screen mounts and by one restart per language; on iOS the same call is expected to serve the views created afterwards).
  - Stage 3 built (2026-09-22, same branch, NOT pushed, NO OTA): the server content in two languages, a dashboard editor for it, the adviser in English, the missing error codes.
    - Server: every block is still stored once, in Arabic; the English version is the same block read through a translation (`server/src/content/i18n.ts`: `Translation<T>`, `wordingOf`, `localize`; only an Arabic text is wording, so links, ids, prices, phones, hours and the Arabic-named logo URLs are shared) plus the owner's edits (`content/edits.ts`, KV `content:edits:<block>:<lang>`, kept apart from the block so a seed bump never loses them; an edit of a path the block no longer has is simply not applied). English seeds in `content/en/*.ts` (7 blocks, 299 texts, written by Claude; the owner corrects them from the dashboard, nobody else reviews them). `get<Block>Content(kv, lang)` everywhere; the public routes read `langOf` (`/api/home | membership | services | golden | about | hq | videos`); the logic keeps the Arabic block (HQ booking rules, Paymob amounts, the adviser's context), and a service-request mail names the service and its fields in Arabic whatever the member's language. English company names used: Wdeny, Virtual Community Company, PV Business Incubators and Accelerators, Seka, Al-Moltaqa, Quick Bite, Quick Deals Trading / Investment, Momentum, Franchment, and «شركة ذبذبات للاستثمار القابضة» as Thabthabat Investment Holding Company: the owner confirms or edits. The wording rule on «الصفقات» now spares the company names «الصفقات السريعة …» (the manfaz text could not be saved otherwise).
    - Dashboard: new section «محتوى التطبيق» (`admin/src/sections/Content.tsx` on the `WordingEditor` shared with `Wording.tsx`): block chips with counts, both versions, search, «المعدَّل فقط», inline edit, «رجوع للنص الأصلي»; `GET | PUT /api/admin/content` behind `dashboardGuard`, the same refusals as the strings editor (empty, 2000 characters, banned wording), and every save moves `/api/sync` `content`, so open apps read the block again within seconds.
    - Adviser: the brain is the hub's n8n workflow, so there is no prompt of ours to translate: the English app says «أجب بالإنجليزية» in the page title (the brain shows its first 90 characters) and at the top of the focus text (`server/src/advisor/context.ts`, `inEnglish`); never a fabricated focus, because a focus routes the message to the deep model. In the app the starters, openings, welcome and gate texts are keys (`advisor.starter.*`, `advisor.opening.*`, `advisor.welcome`, `advisor.gate.*`); the hub's Arabic welcome, quick menu and bot name are replaced by the app's own in English through `hubText`. Seven more `errors.code.*` keys (not_configured, too_large, otp_required, not_published, no_devices, invalid_token, bad_request); 570 keys now, catalog regenerated.
    - Verified: server `npm test` 168 pass (new `content/content.test.ts`: every English seed covers every Arabic text within the copy rules, localize, the routes in both languages, the admin edit flow; `advisor/context.test.ts`); server, app and admin typecheck; app lint on the touched files; admin build. Dashboard in the browser against the local mock server: 299 texts in 7 blocks, an English edit of «Club HQ» saved with the «معدَّل» pill. App on the emulator (debug APK + Metro + `api-local-news`), English: gate, sign-in, home (hero, portals, golden block), adviser welcome and starters, services with SAR prices, membership intro, and the HQ page showing the dashboard's edit live. NOT verified: the brain's actual English reply (needs the live hub), iOS, the release APK.
    - Next (stage 4): the English store listing (M8); the build only if the release APK refuses the direction change; M24's phone-frame preview. Before that the owner reads the English marketing copy in «محتوى التطبيق» and corrects what he wants.
  - Stage 4, part 1 (2026-09-22, right after stage 3 went live: main merged as `8a9213f`, Railway deployed, OTA group `e77cd7ae` on channel preview, runtime 1.0.0). The owner's phone still ran Arabic and forgot the language and the session at every restart. Cause, checked with `eas build:list`: the only preview build is the M1 build of 2026-09-11 (commit `0fb6ba9`, version code 1), made before `expo-secure-store` (M3), `expo-notifications` and `react-native-purchases` (M7 B) entered the app, so on that binary every setting and the session live in memory only, and no OTA can add a native module. Also seen: on runtime 1.0.0 the language link is not on the sign-in gate (`languageChoiceOffered(false)`), only in «حسابي» for admins, by design. Decision (the owner: «نفذ»): a new preview build. Changes for it: `app.config.ts` version `1.1.0` (its own runtime; `UNPROVEN_RUNTIMES` keeps 1.0.0, so 1.1.0 offers English to everyone and follows the device language at first launch), expo-localization without `forcesRTL` (supportsRTL stays; the direction is set by `syncDirection()` before the first screen, one restart when it changes), `extra.forcesRTL` gone, and `autoIncrement: true` on the preview profile so the APK installs over the old one. The owner runs `eas build --platform android --profile preview` and installs the APK over the old app (same id). After it: check that Arabic still lays out right-to-left on a binary that no longer forces it, that English switches with one restart and survives a cold start, that the session survives a restart, and that the push token registers. Then the rest of stage 4: the English store listing (M8) and M24's phone-frame preview.
  - Stage 4, part 2 (2026-09-22, the build 1.1.0 worked on the owner's phone; his next word: «كل المشاريع عربي مع اني مختار اللغة انجليزي»): the Projects Bank in English, server only, no OTA. `server/src/projectsBank/lang.ts`: `localizeProject` puts the founder's English fields (`titleEn`, `companyNameEn`, `excerptEn`, `detailsEn`, published by the site for the projects whose founders filled them) in the place of the Arabic ones and keeps the Arabic where there is none; the site's Arabic sector terms get English names from `SECTOR_EN` (the 18 live sectors; an unknown one keeps its Arabic name), the five steps from `STAGE_EN` with «adviser's estimate» as the mark; `localizeFilters` (sectors, stages, sort labels) and `localizePage`. The routes `/api/projects`, `/filters`, `/:id` and `/:id/brief` read `langOf`. The brief (`brief.ts`) is written in the app's language: the same competitors (chosen on the Arabic data), the rules' sentences from a `TEXT` table per language, the model's prompt with an English language line (never «free», ranges in words), `fixWording` per language, cache key `projects:brief:<id>:en` beside the Arabic one, `BRIEF_DISCLAIMER_EN`. Search already indexed the English fields. Tests: `lang.test.ts` (fields, fallbacks, terms, filters, the English brief and its separate cache); 171 pass. NOT changed: the adviser's context text for the brain (Arabic data; the brain answers in English through the page title), and `contactRule` (never shown).
- Owner's directions of 2026-09-22 (sent while M27 stage 1 was being built; each is its own session unless noted):
  - (1) Order everywhere: «محايد» first, then «رائد الأعمال», then «المستثمر». Done in the app and the server on the M27 branch (type unions, schemas, the mock hub's message; the visible lists already had it). DONE in the hub plugin 2.7.1 and the n8n workflow v4.2 (2026-09-22, see the plugin session below); Projects Bank never lists the three categories.
  - (2) The word is «فئة», never «صفة» («فئتك في النادي»). Done in the app (register screen, interests suggestion). DONE in the hub plugin 2.7.1 (widget sign-up label, step title, the two error texts, the registration mail line «الفئة», the adviser's system prompt; «الوظيفة أو الصفة» became «الوظيفة أو المنصب») and in the n8n builder (`docs/hub-plugin/n8n/build-v4.mjs`: «فئته في النادي التي اختارها عند التسجيل», the map in the owner's order, workflow name v4.2).
  - (3) Plugins (hub «Vibes AI Assistant» + Projects Bank): the same ideas exactly (order, «فئة», the project shown alone). Projects Bank on the websites: a project's «التفاصيل» no longer shows the founder's description; the adviser fills that place with its own table (the app's `ProjectBrief` is the model). Delivery, his words: «عدل وحط في نفس الملف new era وامسح القديم» = write the new zips into the SAME folder `…Ai AgentNew Era` and take the older copy out of it (move it to the Recycle Bin, never a hard delete). Start from his LATEST zips (he edits the plugins in other chats) and run the test rig before anything goes to a live site. DONE 2026-09-22 (plugin session below); the owner uploads.
  - (4) M26 widened: the websites' widget «المستشار» needs a real UX pass (the writing box is often far too small; check voice end to end; fix whatever is broken at once), and the brain must become much smarter: classify visitors well, know which page the visitor is on and analyse it, answer anything about it at once. «ده العقل اللي ماسك كل شئ… تمهيدا لعرض كل ده في ابلكيشن»: design it once for the sites and the app.
  - (5) M11 «شخصية ومسيرة» is still owed in the app (he asked again).
  - (6) M24 clarified: only he, as admin, edits any wording of the app from the dashboard, Arabic or English, nothing more than wording. The first editor shipped with M27 stage 1; the phone-frame preview is what remains.
  - (7) Sign-in gate (owner, 2026-09-22, with his OK for M27 stage 2: «خلي تسجيل الدخول في ابلكيشن اساسي عشان يفتح وفي الدخول كضيف… جلسة كضيف ترجعه تاني يسجل حساب اجباري بعد ما يفتح بدقيقة بحد اقصي»): the app opens on a gate (sign in, create account, «المتابعة كضيف»); a guest session lasts one minute at most, then the gate returns and only an account closes it. Same message: «في حاجات مش مظبوطة انها عربي بيبقي في مشكلة في محاذاه» = look for Arabic alignment faults while the direction spots are converted. DONE in M27 stage 2 (2026-09-22): the gate, the guest minute and both alignment causes; details under M27.
  - (8) Adviser balance (owner, 2026-09-22, while stage 4 was running: «ألغي رصيد المستشار دي خليها تظهر في حالة ان الشخص استخدمه مدة طويلة جدا… خلي السحب بطئ جدا… مش عايز حد يحس ان رصيده خلص»): the app must NOT show the adviser balance («رصيد اليوم» chip in the adviser header, «رصيد المستشار اليوم … من …» in the membership card) unless the member is close to the end of it; the deduction must be very slow, so a member uses the adviser freely and never feels the balance ran out. The balance is the hub's per-contact `daily_limit` / `daily_left`, deducted by the hub plugin one per message: the app part is JS (hide unless little is left), the rate is either the plugin's daily-limit setting or a plugin change (one unit per several messages). DONE (app side, 2026-09-22): `app/src/lib/advisorBalance.ts` shows the balance only when `left <= limit * 0.1`, used by the adviser header chip and the membership card; both hide it otherwise. The slow deduction stays the owner's plugin setting («رصيد العضو اليومي» to 2500).
  - (9) Widget sign-up form on the websites (owner, 2026-09-22, screenshot of the hub widget): the country-code picker must show each country's FLAG next to the code, compact («مياخدش مساحة كبيرة»), and the writing box must be bigger so he types comfortably (the composer complaint of M26 again, now with a screenshot). Plugin work (hub «Vibes AI Assistant»), own session, through New Era and the rig.
  - (10) The brain's answer about a project (owner, 2026-09-22, screenshot): «لخص مشروع» came back as «label | value» lines drawn as plain text («المشروع | مشّيني — Mashini», «الشركة/المؤسس | … المؤسس: سلطان الحربي», «المرحلة | أخرى»…): «الكلام كان مكسر وغريب جدا». Three rules, his words: (a) never the founder's name, anywhere («ظهرلي برده اسم المؤسس وانا قلتلك مش عايش كده خالص»); (b) whoever asks about a project gets ALL its information as one very elegant TABLE, organised in sections («جدول شيك جدا فيه كل المعلومات متقسمة وشيك»): a rich block the widget renders as a real table, not pipes in prose; (c) no «أخرى» as a stage: the brain places the project on the five steps like the app and Projects Bank 39.1 do. n8n prompt/workflow + widget rendering (M26), own session; the app's adviser shows the same rich block when it lands there.
  - (10b) Table rendering rule for the widget (owner, 2026-09-22 05:00, screenshot: the brain now answers a project question with a real table, but the label column is squeezed and breaks words: «القطا/ع», «المرحلا/ة», «السو/ق»): «اكتب قاعدة تحل المشكلة دي». RULE, to apply in the hub plugin's `assets/widget.css` (the rig copy `C:/Projects/vibes-test-rig/hub/vibes-ai-assistant`, lines 127–130 today) in the M26 plugin session: (1) a table cell NEVER breaks inside a word: label cells (`td:first-child`, `th`) get `white-space: nowrap; word-break: keep-all; width: 1%` (the column shrinks to its longest label, at most 40% of the bubble), the value column gets `overflow-wrap: anywhere` and takes the rest; (2) inside a widget narrower than about 480 px (every phone, and the desktop launcher at its default width) the table is drawn STACKED, not in columns: each row is a block, the label on its own line in bold on the soft background, the value under it in full width, rows separated by the thin line; no horizontal scroll ever, the font never smaller than the bubble text (13.5 px), the same in RTL and LTR (`text-align: start`); (3) the table keeps the bubble's rounded frame and the 8 px margins it has now. Same rule for the app's adviser when that rich block reaches it (M26). Also seen in the same screenshot and already in (10): the row «المرحلة: مرحلة التوسع (Expansion)» is the site's raw wording; the brain must name the club step («نمو وتوسع») and never «أخرى».
  - (11) App project brief table (owner, 2026-09-22): drop the row «النوع: مشروع في بنك المشاريع» («مكتوب الفرع بنك المشاريع حاجه زي كده شيلها») and anything like it: the table carries information about the company only. Server `brief.ts` `factRows`, both languages, brief cache VERSION bump. DONE (2026-09-22): the `النوع / Type` row and its two values are gone from `factRows` and from both language tables, cache VERSION 4 → 5.
  - (12) Projects Bank ↔ app, direct (owner, 2026-09-22): a project deleted on the site must vanish from the app at once, and the project's stage must be the site's stage exactly. Facts: the server already refreshes the feed and bumps `/api/sync` `projects` on the PB webhook `project.changed` (`webhooks/routes.ts`), so a deletion reaches open apps within seconds WHEN the PB plugin on vibesholding sends it (bridge v2, plugin 39.x with the bridge key set); without it the feed is polled every PB_REFRESH_MINUTES (10 on Railway). On 2026-09-22 `/health` shows `bridge.pb.ok: false, version: null` on Railway: the owner has not uploaded Projects Bank 39.1 from New Era yet (or the bridge check has not run): upload it, then read «حالة الربط» on the dashboard home and delete a test project to see it vanish.
  - NEXT SESSION (agreed 2026-09-22 at the end of the M27 stage 3/4 session): first the two small app/server items with one OTA and one deploy: (11) drop the «النوع» row from the brief table, (8) hide the adviser balance unless less than 10% is left (the owner raises «رصيد العضو اليومي» to 2500 in the hub plugin settings himself). Then a separate M26 session for the plugin and n8n work of (9) and (10) through New Era and the rig. The owner still has to: push main (two commits were waiting when the session ended: Projects Bank in English and this docs entry), upload the three New Era files, and then check «حالة الربط» for (12).
- **Plugin session (2026-09-22, the owner: «البلجن الأول وبعدين كمل الباقي»… «في مشكلة ان كله ظاهر اخري… مش مسنك صح مع بنك المشاريع»… «اي مشاكل شايلها حلها عشان تحسن تجربة ui و ux»… «رسايل الإدارة دي مهمة جدا تكون اول حاجه»)**. Delivered into `…Ai AgentNew Era` (one copy of each file, the older ones in the Recycle Bin, new steps file `خطوات-التشغيل-2.7.1.md`): `vibes-ai-assistant-2.7.1.zip`, `projects-directory-pro.zip` (39.1), `Vibes_Web_Agent_v4.2.json`. NOT uploaded to any live site (the owner's step). Diffs against his 2.7.0 / 39.0: `docs/hub-plugin/vibes-ai-assistant-2.7.1.diff`, `projects-directory-pro-39.1.diff`.
  - «كله ظاهر اخري», the cause: it is the SITE's own data, not the sync. 57 of the 260 published projects carry the stage term «اخري» on vibesholding.com (the newest ones mostly: 5 of the first 6 cards), and the website shows «اخري» for them too. Fix, one rule in both places (`server/src/projectsBank/stage.ts`, `inc/brief.php` in Projects Bank): «اخري» is not a stage. The adviser reads the step from what the description says ALREADY exists, clause by clause (plans, goals and «القادمة» never count), and marks it «· تقدير المستشار»; a project it cannot read shows no stage at all. On the live data: 12 of the 57 get an estimate, 45 show no stage, none shows «اخري» (founders' texts are mostly marketing prose; the AI brief still reads the stage when a member opens the project). In the app the stage is now one of the club's five steps everywhere (`stage.slug` = `idea | prototype | launch | revenue | growth`, `stage.estimated`), the stage filter is the five steps in their order (the site's long names «مرحلة المنتج الأولي البذرة ( seed )» are gone from the chips), brief cache `VERSION = 4`. The website keeps the site's wording for declared stages.
  - Projects Bank 39.1: `inc/brief.php` = «ملخص المستشار» in the place of «تفاصيل المشروع» on the project page (both templates): summary, fact rows + the founder's own «label: value» lines, the five-step stepper, strengths, «نقاط تستحق الانتباه», the disclaimer, and «اسأل المستشار عن هذا المشروع» (`window.VAI.ask`, shown only when the widget is loaded). Built inside the plugin from the published fields: no AI call, no request to our server (the live site must not depend on the TEST server). Never in it: founder, contacts, the ask, valuations, returns. Arabic and English. Setting «صفحة المشروع — جدول المستشار»: table (default) | table then the founder's text | the old page. The feed, the bridge, the gate and every key are untouched. Told the owner: hiding the founder's text leaves less unique text per page for search engines; the second option brings it back.
  - Found on the way and fixed (rule 4): the API sent a founder's free text as written, so a phone, an e-mail, a link or a «المؤسس: …» line typed INSIDE the description reached the app (2 of the 260 live projects carry such text). `mapper.ts` now passes `details` / `excerpt` (both languages) through `stripContacts` and drops founder lines; test in `mapper.test.ts`.
  - «رسائل الإدارة» is now the FIRST thing on the app's home, above the hero (`app/src/app/(tabs)/index.tsx`; he asked twice). It renders nothing while there are no posts. JS only, goes out with the next OTA.
  - Verified on the rig (`C:/Projects/vibes-test-rig`, rebuilt from its README; new: `wp-test/phase-c2-brief.mjs`, `scripts/sat-brief-seed.php`, `scripts/set-wp-option.php`; start the local API with `cd server && node --import tsx C:/Projects/vibes-test-rig/run-local-api-live.mjs`): fresh database → his 2.7.0 / 39.0 + seed → upgrade in place: phase A 33/33, B 125/127, C 46/46, D 51/51, C2 17/17, n8n simulator 27/27 on v4.2, both zips installed through WordPress's own `Plugin_Upgrader` and C2 again 17/17, static checker clean, PHP logs empty, the sign-up step and the ask button looked at in the browser (desktop and phone width). Server `npm test` 162 pass; server and app typecheck, app lint. The 2 failures of phase B (`series.verified`, `series.activations`: back-dated rows counted as today) fail the same way on his UNTOUCHED 2.7.0, run between 00:00 and 03:00 Riyadh time; not looked into yet: check the daily series around midnight in the M26 sessions.
  - Not done, belongs to M26 (its own sessions): the widget's composer. On the rig it measures 257 × 49 px on desktop and the full width on a phone, and grows to 160 px while typing, so «صندوق الكتابة صغير» could not be reproduced there; look at it on a live site with its theme's CSS. Next session («الباقي»): M27 stage 2, auth and register screens first.
- **The next phase («المرحلة الجديدة»), the owner's list of 2026-09-22 at 05:00 («ضيف كل ده من فضلك عشان ابدا في المرحلة الجديدة»). Each is its own milestone and its own session; nothing started. Decide the open points with him at the start of each.**
- [x] **M28 — Two admin levels: Moderator and Admin** (owner: «موديريتور اللي ارقيه يكون للنشر فقط ولكن التحكم الكامل هو اللي يقدر يرسل رسالة Admin، كل ده شغل داشبورد»). Moderator = publishing only (posts); Admin = full control, the only one who sends «رسالة Admin» (M29). Extends M25: the server already has `role: member | publisher` (`/api/admin/accounts/:id/role`) and the dashboard's «ترقية»; rename/extend to moderator + admin with the dashboard sections each level sees. DONE (2026-09-22): no hub plugin change needed — the contact payload already carries `role` and the hub `publish` op accepts publishers since 2.7.0. Server: `Me.isModerator` (hub role `publisher`), `/api/admin/auth/login|verify` admit admins and moderators (same OTP), `dashboardGuard(auth, allowModerator)` — only the posts routes and `/api/admin/uploads*` pass `true`; every other dashboard endpoint answers a moderator 403 «هذا القسم لحسابات الأدمن فقط»; the app's `/api/admin/hq/*` keeps adminGuard (admins only). Dashboard: a moderator sees only «المنشورات» (any other address lands there, member sheet admin-only), rail shows «— موديريتور» next to the name; labels renamed from ناشر to موديريتور (accounts filter, pill, «ترقية إلى موديريتور»/«سحب صلاحية الموديريتور»). Tests: `server/src/m28.roles.test.ts` (177 total pass). Verified in the browser against the rig mock (port 3009): seeded publisher demo6 signed into the dashboard with the code, saw posts only, published a post that reached the hub; an admin account still sees all 11 sections. Remaining for the owner: push main, Railway redeploys server + dashboard.
- [x] **M29 — Targeted «رسائل الإدارة» and a member inbox** (owner: «يوصل لكل شخص رسالة، لو حابب اكلم عضو معين على انبوكس بتاعه… وأقدر من خلال ابلكيشن اني ابعت لكل الأعضاء او للمحايدين فقط او رواد الأعمال فقط او مستثمرين فقط… واقدر ابعت رسالة فقط لعضو معين»). Audience of a message: everyone, one category (محايد / رائد الأعمال / المستثمر, in that order), or one member; each member has an inbox in «رسائل الإدارة» showing what was sent to everyone, to his category and to him alone; sending from the dashboard AND from the app (an admin composer for admins only); push notification per audience (posts already notify devices). Server: posts get an `audience`; the feed filters by the member's persona and id.
  - DONE (2026-09-22, this session; server + dashboard + app JS, nothing deployed yet). Server: `audience` on every post (`{type:'all'|'persona'|'member'}`, old posts = all; `posts/service.ts` `canSee`), `/api/posts(+/:id)` reads the viewer through `optionalSession` — a guest or the wrong member gets the public posts / a 404, the feed sends only the audience KIND to the app (never the target's id or name). Targeted posts NEVER reach the hub (`hubSync.afterSave` treats them like drafts; retargeting a public post pulls it off the websites, `hubSync.published:false`). Push per audience: the token registry stores the account's persona at registration (`push/service.ts`, filled on each app launch; tokens from before M29 learn it on the next launch — until then category pushes skip them), notify sends to all / one persona's devices / one member's devices, per-audience «لا توجد أجهزة» 409s. M28 guard: a moderator publishes «للجميع» only — targeted create/edit/delete/notify 403, and `/api/admin/posts` hides targeted posts from him. App composer endpoints (adminGuard, app sessions): `GET /api/admin/app/members?q=` (hub `admin_accounts` search) and `POST /api/admin/app/messages` (creates the published message + pushes its audience in one act). Dashboard: «الجمهور» fieldset in the post editor (admins; member search over `api.accounts`), audience pill + «داخل التطبيق فقط» pill in the list, audience-aware notify confirm. App: audience badge «لك خصيصًا» / «لفئتك» on PostCard and the post screen, posts/post/card queries dropped on sign-in/out (inbox belongs to the account), composer screen `posts/compose` (audience chips in the club order + member search) behind an admins-only button on the posts screen; i18n group `compose` (ar+en). Tests `m29.messages.test.ts` (6). Verified in the browser against the local mock (editor picker, member search, targeted publish, pills; guest vs member feed checked over the API).
  - Push detail: `PushToken.persona` is a cache of hub data next to the token (rule 2 intact — the hub stays the registry); a persona change reaches it on the member's next app launch.
- [x] **M30 — Membership card** (owner: «كارت عضوية لكل عضو… رقم مكون من 10 ارقام… شكله عضوية حقيقية… مكتوب عليه رقمه واسمه… الرقم C-0000 اول حرف يدل على الفئة والأرقام الباقية متغيرة… بيعد عليه كل يوم عدد ايام العضوية الباقية… مكتوب عليها عضوية نادي المستثمرين بشكل شيك جدا ومميز… فيها رائد اعمال ولا محايد ولا مستثمر… والوظيفة… واوبشن الطباعة: يختار طباعة توصله لحد باب البيت بدون رسوم بس لازم يختار… انجليزي Membership No.»). A real-looking card in the app: club name, member name, category, job title (`jobTitle` exists on `Me`), membership number = one letter for the category + digits (the shape he wrote is `C-0000`; the count he said is 10 digits: agree the letters per category and the digit count with him, and where the number lives: the hub contact id padded, or a number the server assigns once and keeps), days left counting down daily (`daysLeft` exists), «Membership No.» in English (the English card too, M27), and a «print and deliver» option: the member asks for a printed card delivered to his door at no charge («بدون رسوم»), the request reaches the management (notifier mail + a dashboard list), and he must choose it himself. Design: elegant, distinctive, gold on black like the HQ pass.
  - DONE (2026-09-22, same session as M29; server + dashboard + app JS, nothing deployed). Open points DECIDED (confirm with the owner, one line each to change): number = category letter + dash + 10 digits from the hub contact id (`N` محايد, `E` رائد الأعمال, `I` مستثمر, `C` no category; e.g. `I-0000000042`) — derived, never stored, one source `auth/service.ts membershipNumber` on `Me.cardNumber`; the print request needs an ACTIVE annual membership (the card screen itself shows for every signed-in account). Server: `card/` module — `POST /api/card/print` (city + full address + optional phone/note, one pending request per member, 409 on a second), `GET /api/card/print` (the member's own), notifier mail «طلب طباعة كارت العضوية» (name, number, address, «بدون رسوم»), dashboard `GET /api/admin/card-requests` + `POST …/:id/done` («تم التسليم», admins only). Dashboard: new section «طلبات الكروت» (rail icon, hint «قيد التنفيذ: n», stacked card at phone width). App: `card.tsx` — the gold-on-black card (double gold frame, club logo + «نادي المستثمرين» + INVESTORS CLUB, `Membership No.` in English in both languages, name, category pill from `persona.*` short form, job title, days-left countdown row; expired/unactivated states + «تفعيل العضوية»), print block (button «اطلب النسخة المطبوعة بدون رسوم» → address form → success/pending notices), entry row «كارت العضوية» in the account tab; i18n group `card` (ar+en), strings catalog resynced (`npm run strings`). Tests `m30.card.test.ts` (5); 185 server tests pass, server/admin/app typecheck + lint clean. Verified in the browser against the local mock: member card number and print request over the API, the dashboard section at desk and phone width. NOT yet seen on a device/emulator: the card screen and composer render (checked by types/lint only) — the owner sees them with the OTA.
  - Owner note after seeing it live (2026-09-22 23:30, «ممتاز ظهر… يبقي مكتوب عليه تاريخ انتهاء العضوية ويكون شكله زي بيزنس كارد شيك جدا ومميز جدا جدا جدا كإنه ذهب والاسم واخد ايفيكيت رائع ومعلومات اكتر عني»): redesigned the same session as a gold business card — expiry date row (`membership.endDate`, spelled-out date) beside the daily countdown, more of the member on it (job title · company line, then city / phone / email small at the bottom, Latin values behind an LRM), the name large in raised gold (bright gold + deep text shadow; no gradient package — a new native module would break OTA on the current binary, so the metal is layered views: engraved gold rings, a diagonal sheen band, corner accents, a five-step metallic divider, gold glow shadow around the card). Strings: `card.validThru` / `card.daysLabel` / `card.daysValue` replace `card.daysLeft` (both languages, catalog resynced). If the owner wants MORE shine later (real gradient gold text), that needs `expo-linear-gradient` (+ masked-view) in the NEXT native build first, then the JS can use it behind a feature gate.
- [x] **M31 — Polls («Poll - استفتاء»)** (owner: «اوبشن اني اعمل Poll في داشبورد واقدر اضيف عدد خيارات برحتي خالص»). The dashboard creates a poll with any number of options (title, options, audience, close date); members vote once in the app (a card in «رسائل الإدارة» or its own screen); live results in the dashboard and, if the owner wants, to voters.
  - DONE (2026-09-22, same session as M29/M30; server + dashboard + app JS, nothing deployed). A poll is a post `kind: 'poll'` in «رسائل الإدارة», so it inherits everything of M29: the audience (everyone / persona / one member), the inbox filtering, the per-audience push button, `/api/sync` — and like targeted messages it NEVER reaches the hub (websites cannot vote; `hubSync` skips kind poll). Decisions taken (one line each to change): polls are the ADMIN's alone (a moderator gets 403 create/edit/delete/notify — «الاستفتاءات لحسابات الأدمن فقط»); a member has ONE FINAL vote — the owner's correction of 2026-09-23 («لو اخترت اختيار مينفعش اغيره»): a second attempt answers 409 `already_voted`, and the app votes in two steps (pick, then «تأكيد التصويت», with «صوتك نهائي» said before and after); per-poll «يرى العضو النتائج بعد تصويته» switch (off = counts stay in the dashboard, even after closing); 2–20 options. Server: `poll {options[{id,label}], closesAt (Riyadh time like events), resultsVisible}` on the post — an edited option keeps its id so its votes survive, a removed option's votes count nowhere and new ids are never reused; votes in `polls:votes:<postId>` (`polls/service.ts`, one changeable vote per contactId); `POST /api/posts/:id/vote` (signed-in, audience-checked, 409 `poll_closed` after the close); the feed/post payload carries a per-viewer `poll` block (counts null until: voted or closed, and resultsVisible; admin list always gets `pollResults`); deleting the poll deletes its votes document. Dashboard: «استفتاء» kind chip (admins), options editor (add/remove, 20 max), close day+hour, results switch, live bars with percentages + «المصوتون: n» on the row, pill «داخل التطبيق فقط». App: poll badge on the card («شارك برأيك» / «سجّلنا صوتك» / «انتهى التصويت»), the post screen votes in two steps — tap to pick, then «تأكيد التصويت» (gold fill bars + percentages once counts open, checkmark on my choice, final-vote wording, closes-at line, guests get a sign-in notice); i18n group `poll` (ar+en), catalog resynced (640 strings). Tests `m31.polls.test.ts` (5); 190 server tests pass; server/admin/app typecheck + lint. Verified end-to-end in the browser against the local mock: created through the editor UI, two members voted over the API (one changed his choice — total stayed 2), dashboard reloaded to «السبت مساءً 2 · 100% · المصوتون: 2».
- [x] **M32 — Invitations with a verified referral** (owner: «طريقة دعوة اي عميل من جوا ابلكيشن يرسل دعوة لأي احد يسجل من خلاله، واي حد يسجل من خلاله ياخد هدية من الإدارة، يرشح بس أعضاء من طرفه يسجلوا، وانت لازم تتأكد انه سجل من خلاله»). A member shares an invitation (link/code) from the app; a registration through it is attributed to him and verified on the server (the code travels with the hub registration and is stored on the new account; never trusted from the client alone); the invitee gets a gift from the administration (the owner defines it: days, balance, a service), the inviter only nominates; the dashboard lists invitations and who registered through whom. Needs the hub plugin to carry the referral (New Era + rig).
  - DONE (2026-09-23, this session; server + hub plugin 2.7.2 + app JS + dashboard, nothing deployed). Decisions (owner's OK on the plan; one line each to change): the invite code IS the membership number of M30 (`I-0000000042`; tolerated with Arabic digits, spaces, missing dash/letter — `normalizeInviteCode`); the inviter must hold an ACTIVE membership; the gift is MANUAL («يدوي حسب احنا هنديله ايه، ممكن كود خصم ممكن هدية كتاب المؤسس») — the dashboard only records it.
  - Where the truth lives: the HUB resolves and stores the referral itself in op_register (plugin 2.7.2: `referral` param → checks the code's owner is a verified account with an active membership, else refuses `referral` 400 before any DB write; stores contacts.referred_by via dbDelta upgrade DB_VERSION 2.1.0; response carries `referred_by`+`referred_name`; the registered system message says «بدعوة من العضو #…»; admin_accounts/admin_member rows carry `referred_by`; widget registrations without a code untouched). The APP SERVER shape-checks first (`invite_code` 400), refuses codes while the live hub is older than 2.7.2 (ping-version gate, 10-min cache: `invite_unavailable` 503), passes the code through, and records the invitation in its KV (`invites:registrations`, `invites/service.ts`) from the hub's confirmed answer — gift status + wording are server-side operational data like card print requests.
  - Server: `invites/` module — `GET /api/invites` (member: code, eligible, giftText, shareText with {code} filled, his nominees with state registered|verified|member), admin `GET /api/admin/invites`, `POST /api/admin/invites/config` (giftText + shareText, KV `invites:config`, defaults in code), `POST /api/admin/invites/:id/gift` (note required — what was handed over; first delivery wins). Hooks: verify() marks the row + notifier mail «تسجيل جديد بدعوة عضو» (once, before the rule-9 gate so a TEST-env refusal still records it); membership activation marks `activatedAt`. Mock hub mirrors the plugin (`referredBy`, `referral` refusals, MOCK_HUB_VERSION 2.7.2-mock). Tests `m32.invites.test.ts` (7); 197 server tests pass.
  - App (JS only, next OTA): `invite.tsx` («ادعُ عضوًا جديدًا» in the account menu: gold code box, RN Share with the server's text, nominees list, membership-required lock), register screen field «كود الدعوة (اختياري)» + hint, i18n group `invite` (ar+en, catalog 663 strings). Dashboard: section «الدعوات» (gift icon) — wording editor + table (invitee, phone, inviter+number, progress pill, date, gift button «تم تسليم الهدية» via prompt); admins only (moderator 403 by the default guard).
  - Verified: rig full run on the real WordPress — phase A 33/33, B 136/138 (the 2 known midnight stats checks, fail identically on untouched 2.7.0; README documents), C 46/46, D 51/51, C2 17/17 twice (before and after installing the 2.7.2 zip through Plugin_Upgrader), NEW `m32-e2e.mjs` 10/10 (app API → live local hub: malformed/unknown code refusals, sara's code stored in SQL, dashboard row, verify marks it, gift + wording save), static checker clean, PHP logs clean. Dashboard section checked in the browser (login, config save, table row). App screens by typecheck+lint only (owner sees them with the OTA), server+app+admin typecheck and app lint clean.
  - Delivered: `New Era/1- بلجن المساعد 2.7.2/vibes-ai-assistant-2.7.2.zip` + `خطوات-التشغيل-2.7.2.md` (old zip and steps in the Recycle Bin; PB 39.1 and n8n v4.2 unchanged). Diff: `docs/hub-plugin/vibes-ai-assistant-2.7.2.diff`. Deploy order in the steps file: plugin first, then push main (Railway server+dashboard), then OTA; either order is safe because of the version gate. Remaining for the owner: upload the zip, push, OTA.
- [x] **M33 — The dashboard, full control and live infographics** (owner: «داش بورد فيها للادمن تحكم كامل في كل شئ… شيك جداااا ومميز جدا جدا جدا وفيه جراف وشغل انفوجرافيك مميز جدا متغير حسب الاحصائيات»). Joins M22 (usage analytics) and M23 (content control): every content block, post, poll, member, service and setting editable from one elegant dashboard; charts and infographics that move with the numbers (members by category, memberships, payments, adviser use, Projects Bank unlocks, news reading), by day/week/month.
  - DONE (2026-09-23, same session as M34; server + dashboard + app JS, nothing deployed). Three pieces:
  - **Analytics (M22)**: app `ScreenTracker` (root layout) + `lib/analytics.ts` reports route names only (`(tabs)/news`, `project/[id]` — never an id or a parameter), batched every 30s / 20 screens / on background, silent failures, device id in secure store; owner sees numbers after the next OTA. Server `analytics/` — `POST /api/analytics/screens` (rate-limited, optional session; visitor = contact id when signed in, else device), one small KV doc per Riyadh day `{screens, events, visitors}` (memory-buffered 15s flushes, 400-day retention via `analytics:index`, visitor list capped, unknown screens fold into `other`), and one `onResponse` hook in app.ts counts `advisorMessages` (POST /api/advisor/message), `pbUnlocks` (POST /api/projects/:id/unlock), `newsReads` (GET /api/news/:id) with no route changes. `GET /api/admin/analytics?days=` (admins) answers the days + members-by-category (`DashboardService.personas`: hub admin_accounts state=member pages, rule 2, cached) + PB unlocks bucketed by day from the bridge's newest 500 (`pbUnlockDays`) with per-part error isolation.
  - **«الإحصائيات» section** (rail after الرئيسية): today tiles (فتحوا التطبيق، مشاهدات، رسائل المستشار، قراءات الأخبار), gold SVG donut «الأعضاء المشتركون حسب الفئة» (club order محايد/رائد الأعمال/المستثمر; mirrored ring for RTL), «أكثر الشاشات زيارة» meters with Arabic route labels, «استخدام التطبيق» chart (metric chips; unlocks prefer the bridge series, fall back to the counter) and «العضويات والمدفوعات» chart (hub signups/activations/renewals/payments + app payments) — all under range chips (30/90/180) AND grouping chips يوم/أسبوع/شهر (`grouped()` buckets weeks from Sunday, months by name; distinct visitors group as the bucket's best day, said on the chart). `BarChart` learned an optional per-point `label` + `dayLabel` column name; `Donut` is new in Chart.tsx.
  - **«القيم والأسعار»** (tab inside «محتوى التطبيق» beside «النصوص», M23 completed): `content/values.ts` walks the seven blocks for the values the text editor skips — numbers (prices, order, valuations), `https://` links, `+…` phones, booleans — as typed rows; `GET /api/admin/content/values`, `PUT /api/admin/content/value` writes the stored block in place (chained, type-checked, updatedAt bumped, sync `content` so open apps refetch in seconds), `null` = «رجوع للقيمة الأصلية» (seed value), sidecar `content:values:edits` records by/at, «معدّلة» pill in the UI. Kept code-side on purpose (the one open piece of «تحكم كامل»): item names/keys/ids, enum fields (action.type, access, group), icon names, and ADDING/REMOVING rows (a new service, featured video, golden company) — each can break app drawing/routing; say the word and it gets its own milestone.
  - Tests `m33.dashboard.test.ts` (9); 211 server tests pass; server+app+admin typecheck, app lint, admin vite build clean. Verified in the browser against the local mock (rig `api-local` + vite): stats tiles/donut/meters with seeded data, monthly grouping on both charts, a meetup member-price edit through the UI reached the store and reset back. App reporter is JS-only → next OTA; until then the usage numbers sit at zero and the section says so.
- [x] **M34 — More news sources** (owner: «حاول تنوع اكبر في عدد الصحف اللي بنجيب منها اخبار»). Widen the Arabic and English source lists (rule 7 unchanged: real items only, fetched pages, the source's own snippet); the owner turns sources on and off from the dashboard.
  - DONE (2026-09-23, same session as M33; server + dashboard, nothing deployed). Seed v3 (`NEWS_SEED_VERSION = 3`; `mergeSeed` adds the new rows to a stored list and keeps the owner's switches — proven in tests). Every candidate probed through the real pipeline first (`npm run news:probe -- file.json`, new keeper CLI: feed fetch + first 3 article pages exactly as refresh() does).
  - New Arabic (11, all enabled, feeds mostly Bing News per-site like الاقتصادية): سبق، عاجل، اليوم، الوطن، المدينة، مكة، البلاد (direct RSS)، مباشر، CNBC عربية، الشرق للأعمال، اندبندنت عربية (global tier, query filtered بالسعودية). New English (11, `saudiOnly` all): The Guardian (direct Saudi-tag RSS), Zawya, Gulf News, Khaleej Times, Economy Middle East, Fast Company ME, Wamda, TradeArabia, Forbes ME, MEED — and Arabian Business DISABLED (article pages 403, hint says so, same pattern as Reuters/Bloomberg/FT/AP). Probed and skipped: alriyadh (RSS 404 + empty Bing), أم القرى feeds (404; the outlet-name map still names it on aggregated links), CNN via Bing (one Spanish-edition hit). Outlet-name maps extended so aggregated links show the outlet, never Bing. Arabic totals now 21 enabled of 25; English 21 enabled of 26.
  - Dashboard section «مصادر الأخبار» (admins; rail after محتوى التطبيق): language chips, counts strip (مشغّلة/معروضة الآن/آخر تحديث), «تحديث الأخبار الآن» button (fires `refresh()` in the background), one row per source — tier pill, hint, last-poll health (in-feed/stored/failed-pages/error or «لم يُقرأ بعد») and an on/off switch. Server: `GET /api/admin/news/sources`, `POST /api/admin/news/sources/:id` `{enabled}` (`setSourceEnabled` rewrites the stored list, turning ON also fires a refresh), `POST /api/admin/news/refresh`; all dashboardGuard (moderator 403 by default). Tests `m34.sources.test.ts` (5). Verified in the browser: list, both toggle directions on سبق.
  - Note for the LIVE deploy: the deployed store holds seedVersion 2, so the merge adds the 22 rows on first boot after push; enabled Bing-based sources raise each refresh's page fetches — NEWS_MAX_NEW_PER_RUN still caps the run.
- [x] **M35 — Projects Bank presentation and direct contact with the founder** (owner: «حسن طريقة عرض المشاريع في بنك المشاريع عن كده، وخليني اقدر من برا كده من اي مشروع اتواصل مع المؤسس بشكل مباشر»). A richer project list and page (image, sector, step, the adviser's table first, M17 polish). OPEN POINT to decide with the owner first: «التواصل المباشر من أي مشروع» meets rule 4 and the balance of 5 unlocks per membership year: either the contact button on every project uses the unlock (one of the 5, then WhatsApp/e-mail as today), or the contact goes through the management/adviser without the founder's data, or the owner changes the unlock rule. Never the founder's name or contacts on the project itself before an unlock.
  - DECIDED (owner, 2026-09-23): the contact button uses an unlock — «تواصل مع المؤسس» on every project deducts one of the 5 (confirm sheet first), an already-unlocked project contacts at no further cost; rule 4 untouched.
  - DONE (2026-09-23, this session; app JS only — no server change; shipped in the preview OTA of 2026-09-23, see M40's live check). List: `ProjectCard` rebuilt as a rich vertical card (wide image with the golden star overlay and the STAGE pill on it, title, company, excerpt, sector chip, views, and a gold «تواصل مع المؤسس» CTA on every non-golden card). The CTA and `autoStart`: the card CTA opens `project/[id]?contact=1`, and `ProjectUnlock` (now titled «تواصل مع المؤسس مباشرة») opens the confirm sheet by itself once the access answer says `can_unlock` — the member still confirms the deduction himself; guests meet the sign-in words, golden projects show the partner-page button instead. Page order (M17 polish): gallery → title/tags → partner button (golden) → the contact/unlock box → **the adviser's brief first** → info card → details → discuss-with-advisor. `unlock.*` strings recast around direct contact in both languages.
- [x] **M36 — Contact the administration, members only** (owner: «العميل يقدر في اي وقت يبعت للإدارة طالما عضو عضوية سنوية في النادي، غير كده ميقدرش يوصل للإدارة»). A «راسل الإدارة» entry in the app for accounts with an active annual membership only (guests and unactivated accounts see the membership screen instead); messages land in the dashboard (a thread per member, replies back to the app inbox of M29, notifications both ways); the WhatsApp handover of services stays as it is.
  - DONE (2026-09-23, this session; server + dashboard + app JS; deployed and OTA'd the same day, see M40's live check). Server `contact/` module: one thread per member in KV (`contact:thread:<contactId>` + `contact:index`, 300 messages per thread, writes chained), the ACTIVE annual membership is the door on both read and write (403 `membership_required` — the app then shows the membership gate, exactly the brief's rule); `GET/POST /api/contact` (member; GET records his look), `GET /api/admin/contact` (summaries with the waiting count), `GET /api/admin/contact/:contactId` (opening clears the waiting count), `POST …/reply` — dashboardGuard, admins only (moderator 403). Notifications both ways: notifier mail «رسالة جديدة من عضو» once per waiting burst (a second message while the first waits does not mail again; an answered thread mails anew), and the reply pushes `contactReplied` («رد من إدارة النادي», screen `/contact`) — PushRegistrar refreshes the open thread on arrival.
  - App: `/contact` chat screen — guest → sign-in words, signed-in without active membership (or a 403 meanwhile) → the membership gate with «فعّل عضويتك السنوية», active → the thread: bubbles (mine gold-tinted, the management's signed «{name} · إدارة النادي»), 20s polling while open, optimistic echo with «جارٍ الإرسال…», composer with the send circle; entry row «راسل الإدارة» in the account tab; i18n group `contact` (ar+en, catalog resynced, 676 strings).
  - Dashboard: section «رسائل الأعضاء» (inbox icon, after «الدعوات»): search by name or membership number, rows with the last words + «n بانتظار الرد»/«تم الرد» pills + persona + number, split thread view (bubbles, member sheet link) and the reply box. Interpretation note: «replies back to the app inbox of M29» is served by the dedicated in-app thread + push (no synthetic posts in «رسائل الإدارة»); one line to also drop a copy there if the owner wants.
  - Tests `m36.contact.test.ts` (4); 215 server tests pass; server+app+admin typecheck, app lint, admin vite build clean. Verified in the browser against the local mock (rig `api-local` + vite, dashboard OTP = mock code 123456): the seeded member's two messages appeared with «2 بانتظار الرد», the reply went out through the UI (toast, counter to 0), and the member's `GET /api/contact` carried the admin reply signed by name.
- [x] **M37 — Projects Bank: no project without a membership** (owner, mid-M32 session 2026-09-23: «مفيش مشاريع بيتم رفعها بدون عضوية، لابد من توفر عضوية حتى يتم عرض المشروع على بنك المشاريع، ولا يتم قبول أي مشاريع بدون التسجيل في عضوية نادي المستثمرين»). A project is accepted and shown on the Projects Bank only when its founder holds an active annual club membership (the vcmem hub stays the one membership source, rule 2). Touches the Projects Bank plugin's submission/acceptance flow on vibesholding.com (New Era + rig). Decide with the owner at the start: how the site checks the founder's membership against the hub (phone/e-mail through a bridge?), and what happens to already-published projects whose founders hold no membership (hide them or keep them) — hiding could empty most of the 260 live projects.
  - DECIDED (owner, 2026-09-23, this session): the 260 existing projects stay as they are («سبهم دلوقتي») — the rule bites NEW publishes only; and «أهم شئ بمجرد ما ينزل علي الويب سايت ينزل فورا في نفس الثانية علي ابلكيشن» (covered: PB 39.x already sends `project.changed` on publish and the server refreshes at once — proven again in the rig, see below).
  - DONE (2026-09-23, this session; **Projects Bank 39.2, NO hub change** — delivered to New Era, the owner uploads): new `inc/membership.php` — publishing a project first asks the hub op `member_status` (already site-facing in 2.7.x) about the founder's e-mail / WhatsApp THROUGH the assistant plugin's own connection (`VAI_Site::client`, the site's own key; the live site never talks to the TEST server); no active membership (or hub unreachable = fail closed) → the project is saved back as a draft with a red Arabic notice; a pass is stamped and re-checked only when the founder's e-mail/WhatsApp changes; every project published before 39.2 is stamped `legacy` once at upgrade and stays exempt. Side metabox «🪪 عضوية المؤسس» with «تحقّق الآن من الهاب», settings box «شرط عضوية المؤسس» (on by default; emergency off switch + hub-connection state), and the welcome e-mail is muted until the gate passes (`publish_project` fires before meta saves). Rig: full run — A 33/33, B 136/138 (the two known midnight stats checks), C 46/46, D 51/51 (bridge now says pb 39.2), C2 17/17 twice around the zip install through `Plugin_Upgrader`, m32 10/10, NEW `m37-e2e.mjs` 9/9 (legacy exempt, unknown → draft, member → published+stamped, `project.changed` reached the webhook inbox on the publish, founder-change re-check, members-list path, missing-data block, toggle), static checker + php -l clean, no PHP errors in the logs. Delivered: `New Era/2- بلجن بنك المشاريع 39.2 (vibesholding فقط)/projects-directory-pro.zip` (same file name rule; 39.1 folder in the Recycle Bin), diff not needed (39.1→39.2 = the new file + 4 lines in the main file). Owner steps: upload hub 2.7.2 first if not yet done (M32), then this zip on vibesholding; then in the PB settings check «متصل بالهاب ✅» and try a test publish with a non-member e-mail.
- [x] **M38 — Liveliness: the app animated everywhere** (owner, 2026-09-23: «ياريت ابلكيشن يبقي animated اكتر من كده وفي روح وتفاعلي اكتر وفيه حاجات بتتحرك كده… حسه مسمط كده ومفهوش اي روح نهائي»). Motion across the app with NO new native module (OTA-safe on the current binary, core `Animated` only): staggered card entrances on the lists, press-scale feedback on buttons and cards, skeleton loading, counting numbers, a moving gold sheen on the membership card, animated poll bars and tab icons.
  - DONE (2026-09-23, this session; app JS only; shipped in the preview OTA of 2026-09-23). One shared file `app/src/components/motion.tsx`: `FadeInView` (fade + slide-up entrance; vertical only so RTL never mirrors) + `entranceDelay` stagger, `PressScale` (spring dip under the finger), `usePulse` + `SkeletonCards`, `useCountUp` (counts from the shown value; lint-safe), `Sheen` (a light band sweeping a gold surface), `GrowBar`. Applied: `AppButton` springs on every press app-wide; `StateView` loading = breathing ghost cards (skeleton), error/empty fade in; tab-bar icons pop on focus; home (hero, portals, golden strip, membership block staggered; golden minis spring); projects (list stagger + the new card, page sections staggered); news list stagger; posts list stagger + `PressScale` on every post card + poll result bars grow to their share; golden screen hero + companies staggered; membership card entrance + the gold sheen sweeping it + the days-left counting itself up; the contact chat's bubbles fade in. Lint note (recorded in auto-memory): eslint react-hooks v6 refuses `useRef(new Animated.Value()).current` — Animated values are created with `useState(() => new Animated.Value())`, the repo pattern.
- [x] **M39 — Golden companies without the per-company valuation** (owner, 2026-09-23: «في المشاريع الذهبية شيل التقييم اللي تحت كل شركة ده»). Remove the valuation row under each company on the golden screen and the home strip; the umbrella portfolio value in the hero and the disclaimer stay; server content and the dashboard values editor keep the numbers (display-only change).
  - DONE (2026-09-23, this session; app JS only; shipped in the preview OTA of 2026-09-23): the valuation row left `GoldenCompanyCard`, the home mini now shows the company code always; `golden.valuation` dropped from both string files; hero portfolio value + disclaimer untouched; the server block and the «القيم والأسعار» editor still hold the numbers, so showing them again someday is one UI line.
- [x] **M40 — Daily news, hands off** (owner, 2026-09-23: «عايزه طبعا يحدث الأخبار لوحده ويبقي في اخبار يومية بشكل دوري مهمة جدا»). The server already refreshes on boot and every `NEWS_REFRESH_MINUTES` (default 20) — the live gap is that M33+M34 (22 extra sources) is not deployed yet. After the owner pushes: verify on the live server that refresh runs and new items land daily; fix whatever blocks it (env, sources).
  - Status 2026-09-23: nothing to code — `news/service.ts start()` refreshes at boot and every 20 minutes, and M34's dashboard section shows each source's last-poll health and «آخر تحديث». Remaining = deploy + live check: after the push, confirm Railway has no `NEWS_REFRESH_MINUTES=0`, watch `/api/news` gain fresh items over a day, and fix any live-only source failures from the «مصادر الأخبار» health column.
  - VERIFIED LIVE (2026-09-23 00:12 UTC, right after the owner's push `822a1b0` + OTA): everything of this session is on the live systems and working. The push landed (origin/main = 822a1b0), the API redeployed (`/api/contact` answers 401 `unauthorized` — the M36 module live), the dashboard at dashboard.vibesholdingsa.com serves the new build (bundle `index-Bp1ik9UY.js` carries «رسائل الأعضاء» and «بانتظار الرد»), and the OTA is on branch `preview` (runtime 1.1.0, «projects contact, message management, animations», by yasservibes). The news refresh runs by itself on the live server: `/api/news/feed` `updatedAt` was 2 minutes old at the check, the newest Arabic item 6 minutes old; Arabic feed 300 items from 14 distinct outlets (واس 140، عكاظ 63، الشرق الأوسط 40، مباشر، الوطن، سبق، CNBC عربية، البلاد، مكة، الاقتصادية، المدينة، الشرق للأعمال، اليوم، عاجل — the M34 additions all present), English 173 items from 14 (SPA, Saudi Gazette, Arab News, Wamda, TradeArabia, Zawya, CNBC, Khaleej Times, MEED, The Guardian, The National, Semafor, BBC, Gulf News). The variety keeps widening as the 20-minute runs poll the Bing-based sources (`NEWS_MAX_NEW_PER_RUN` caps each run); the owner watches per-source health in «مصادر الأخبار».
- [x] **M41 — «أجندة النادي»** (owner, 2026-09-23, after M37 went live: «عايز اضيف حاجه اسمها أجندة النادي يكون فيها كل الفعاليات اقدر ادخل عليها واسجل حضور بدوسة زرار بسيطة جدا ولو في رسوم يدفعها لو مش عضو، لو عضو يسجل حضور بمنتهي السهولة يختار بس اونلاين ولا حضوري مش اكتر. ويكون في الأجندة دي فعاليات اضيفها طول السنة… وده قسم مهم جدا»). Events across the whole year, added from the dashboard at any time; ONE-tap attendance in the app.
  - DONE (2026-09-23, this session; server + dashboard + app JS, one OTA after the push). Server `server/src/agenda/` (KV `agenda:events` + `agenda:registrations`): an event = title, blurb, Riyadh day + من/إلى times, place, hidden online link, mode (حضوري/أونلاين/both), `feeSar` for NON-members (members always بدون رسوم; 0 = free for everyone), open flag. `GET /api/agenda(/:id)` is viewer-aware (my registration state, `mustPay`, and the online link ONLY for a confirmed online attendee); `POST /api/agenda/:id/register {attendance}`: a member or a no-fee event is confirmed at once (+ notifier mail «تسجيل حضور»); a signed-in visitor without an active membership gets a Paymob checkout from the SAME payments module (new shared `createPayment` core + `startAgenda`, key `agenda:<eventId>`) and his registration flips paid ONLY through the HMAC webhook (rule 5) via the new `payments.onSettled` listener → confirm + mail + push «تم تأكيد حضورك»; an unfinished payment re-register starts a fresh checkout instead of a 409; mode/closed/past all enforced. Admin: CRUD + per-event registrations (attendance/paid/member) + «نبّه الأعضاء» push broadcast (`/agenda/<id>` deep link). Changes bump sync `content`, so open apps hear new events in seconds.
  - Dashboard: section «أجندة النادي» (calendar icon, after «رسائل الأعضاء») — the year's events table (date, mode, fee, confirmed/hq/online/awaiting-payment counts, open pill), editor sheet (date/time pickers, mode, fee, online link, open switch), registrations sheet, notify + delete. App: `agenda/index` (date-badge cards, my-state pill), `agenda/[id]` (details, fee note, one-tap buttons «سجّل حضوري»/«سجّل أونلاين» — with the fee on the button for payers → checkout → `/payment/<id>` as services do; confirmed card + «رابط الحضور أونلاين»), home strip `AgendaBlock` after the portals, account-tab row, and the guide's workshops step now opens the agenda (one system for every event). i18n group `agenda` (ar+en), catalog 741.
  - Verified: `m41.agenda.test.ts` (7) — 232 server tests pass; server/app/admin typecheck + lint + build clean; on the local mock: event created and listed publicly (fee shown, online link hidden), and the dashboard section rendered the row with its counts in the browser. The fee flow end to end (checkout → signed webhook → confirmed + link revealed) is covered by the unit tests through the mock gateway's real HMAC path.
  - To watch on the first REAL fee payment (Paymob test keys, a device): the checkout return → `/payment/<id>` → the agenda screen showing «تم تأكيد حضورك». Note: Paymob live intention creation was still answering 404 for the test integration (owner's support ticket, M7 part 2 A) — until Paymob fixes it, Railway runs the mock gateway and fee payments are test-only.

- [x] **M42 — Automatic notifications** (owner, 2026-09-23, right after the FIRST real push arrived — Railway Hobby paid, the FCM V1 key generated from Firebase project `vibes-holding` (the SECOND Google account; the leaked Sep-20 key deleted from GCP, the key file recycled off OneDrive), uploaded to expo.dev by Claude driving the pane, `/health` → `push.sent: 2, failed: 0`: «عايز الأخبار برده ترسل اشعار واي حاجه يضيفها النادي… بمجرد ما انشر اي رسالة كل الناس يجيلها اشعار فورا»). DONE (2026-09-23, this session; server only, no OTA): a post's FIRST publish notifies its audience by itself (edits never re-notify; the manual button stays; recorded as notified like the button's send); a NEW open agenda event notifies every device; and every NEW item of «قرارات وأنظمة المملكة» notifies with the source's own title (rule 7 — nothing written by us), capped at 3 a day (Riyadh) with a first-run baseline so a deploy never floods old items (`push:news:decisions`). `AUTO_PUSH=0` on Railway switches all of it off. Tests `m42.autopush.test.ts` (5; m9 now runs with AUTO_PUSH=0 to keep testing the manual path).
- [x] **M43 — One-time AI English for the owner's content** (owner, 2026-09-23, with two phone screenshots of English mode showing his Arabic posts/events: «عايزك تستخدم AI انه يترجم كل حاجه مرة واحدة وانا لو لقيت مشكلة في الترجمة ابقي اعدلها»). DONE (2026-09-23, this session; server + dashboard, no OTA — the app already sends `x-app-lang`): `server/src/translate.ts` (OpenAI, strict JSON, keeps names/numbers/URLs, never «free», 20 s timeout, failure = keep Arabic till next edit; OFF without `OPENAI_API_KEY`). Posts and agenda events get `en {title, body/blurb, place}` + `enAuto` written ONCE at save and rewritten only while the Arabic changes AND the owner never touched the English; the dashboard's new English fields (post editor + agenda editor) auto-fill and an edit there wins for good (`enAuto: false`). The English app reads the stored English server-side with Arabic fallback. NEWS is never translated (rule 7 — per-language sources as before). NOTE: items saved BEFORE M43 have no stored English — opening and saving one from the dashboard translates it. Not translated yet (say the word): poll option labels, «شخصية ومسيرة» profiles. Tests `m43.translate.test.ts` (3); 240 server tests pass.

- [x] **M44 — «روابط الدفع» with instant automatic activation** (owner, 2026-09-23, urgent, with the hub's «دفعة ناجحة بمبلغ غير معروف في جدول المبالغ (1,900 SAR) من NA NA» mail: «اعضاء عمالة تسجل ولازم افعل التسجيل يدوي… محتاج تربط بايموب بينا مباشر بحيث يتم التفعيل فوري واعرف اللي دفع ده من الورشة ولا عضوية… بالكامل اوتوماتيك»). Diagnosis: those payments come from the SITE's Paymob links — amounts outside the hub's amounts table and links that carry no customer identity. DONE (2026-09-23, this session; server + dashboard, no OTA, rules 3/5/6 intact — dashboard-only, HMAC-webhook-settled, the site's own callbacks untouched):
  - Dashboard section «روابط الدفع» (admins only): create a link — kind (عضوية سنوية / ورشة / أخرى), label, amount, days (membership), customer name/phone/e-mail — a MEMBERSHIP sale is matched to the hub account at CREATION (`member_status`, now in our hub client/mock too) and says so; copy-link + ready WhatsApp text; the list shows paid/waiting/failed + the activation state live.
  - Server `server/src/paylinks/` on the payments core (new `startLink` + customer-shaped `createPayment` + `byId`; generic paid/failed mails are silenced for `link:*` so ONE clear mail tells the story): the paid webhook mails «✅ دفعة {نوع}: {وصف} — {اسم} ({مبلغ} ريال)» with the activation line, and a matched membership sale calls the hub `activate_member` (ref `paylink:<paymentId>`, idempotent) INSTANTLY + `auth.forget` + the member's push «تم تفعيل عضويتك» + the invites gift hook; unmatched → flagged «فعّل يدويًا»; a failed attempt mails once and the link stays usable.
  - Tests `m44.paylinks.test.ts` (5) — 245 server tests pass. LIVE CAVEAT: Railway still holds the Paymob TEST keys and integration 33439 whose `/v1/intention/` answered 404 (the owner's old support ticket) — REAL money through these links needs the LIVE Paymob keys + a live integration id (e.g. 33090) as Railway variables (the owner pastes them; our per-payment `notification_url` stays ours — rule 6 untouched), then a small-amount live test. UPDATE (2026-09-23, later): the owner pasted the LIVE keys on Railway — `/health` shows `keys: live`, integration **28390**; the small live test (5 SAR paylink paid by the owner) is still pending. If money is taken but the link stays «في انتظار الدفع», suspect `PAYMOB_HMAC_SECRET` (he changed only secret/public/integration).

- [x] **M46 — Social sign-in in seconds (Google on Android, Apple on iOS)** (owner, 2026-09-23: «عايز يكون في تسجيل في ابلكيشن بجوجل علي اندرويد و iCloud علي IOS بحيث التسجيل ميكلمش معاه 3 ثواني لازم نضيف الحتة دي ولو نقدر كمان في بلجن بتاعة الويب سايت اللي مربوطة بالمنظومة ياريت»). CODE DONE (2026-09-23, this session; see docs/BRIDGE_V2.md 1.3 + 5.2). **CLOSED 2026-09-23 night: the owner installed preview build #3 on his Android and confirmed the Google sign-in works live («كله حصل»).** Deferred parts ride other milestones: Apple button = first iOS build (M7, + web iCloud needs an Apple Services ID), production Android client id (Play App Signing SHA-1) = store build (M8).
  - Server: `POST /api/auth/social {provider, token, name?}` — `server/src/auth/social.ts` verifies the ID token ITSELF against Google/Apple JWKS (signature, issuer, audience from env `GOOGLE_CLIENT_IDS`/`APPLE_APP_IDS`, expiry, e-mail) → hub op `social_login` (allow_create = registrationOpen; rule-9 gate as password login) → same `{token, me}`. `/api/auth/config` gains `social{google,apple}`, `/health` `hub.social`. Tests `m46.social.test.ts` (8, real RS256 JWTs against an injected JWKS); 253 server tests pass; build clean.
  - Hub plugin 2.8.0: trusted-only op `social_login` — finds the contact by e-mail (password or not) or creates it VERIFIED at once (no code mail); a passwordless contact gets a random unusable pass_hash so `has_account=1` everywhere (the rig caught that: without it `/api/me` kills the session; the member can «نسيت كلمة المرور» any time); admin notify + lead on creation, login mail on sign-in.
  - App: `app/src/auth/social.ts` (registry-gated native modules — OTA on old binaries hides the buttons), `SocialButtons` on login + register (Google on Android, Apple on iOS), i18n `auth.social.*` (catalog 745), `expo-apple-authentication` + `@react-native-google-signin/google-signin` installed, `usesAppleSignIn`, extra `googleWebClientId` (public; from `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`). Typecheck + lint clean; config plugins introspect clean.
  - Console (2026-09-23 evening, Claude driving the pane, account megavc2024@gmail.com authuser=1, project vibes-holding): OAuth consent CONFIGURED (app «نادي المستثمرين», support+contact e-mail set, External) and BOTH clients CREATED — WEB `980470249678-g51bg4r3u4op57tse8t2nt4vut1fp0cm.apps.googleusercontent.com` (its client secret is UNUSED in our flow — ID-token verification only, no code exchange) and ANDROID `club-android-preview` (`com.vcmem.investorsclub.preview`, EAS keystore SHA-1 `CB:78:56:F8:0A:FB:5D:43:EF:C9:A6:EF:FD:6E:0C:1C:14:08:A1:A8`). The web id is already in eas.json env (preview+production). Angular-console gotcha for next time: the visible UI is window.top (pangolin iframes are 10×10 ghosts); synthetic clicks WORK for most buttons but «Save» on chip inputs and «Publish app» did not stick, and the auto-mode classifier blocks the publish confirm anyway.
  - STATUS (2026-09-23 night, follow-up session): the old go-live steps 1–4 are ALL DONE — hub **2.9.0** live on vcmem (carries `social_login`), consent screen In production (M49 session), Railway holds `GOOGLE_CLIENT_IDS` + `APPLE_APP_IDS` (`/health` → `hub.social {google:true, apple:true}`, `/api/auth/config` → `social` both true), and preview **build #3** (v1.1.0, 2026-09-23 16:08 UTC, commit 6bd0da0, APK on expo.dev) already CONTAINS the social code (5e53ff1 is an ancestor) + `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` in eas.json (preview+production) — NO new build needed. Emulator-verified on the real build-#3 APK (this session): installs and boots clean (first binary with the native modules — no registry crash), Arabic RTL login shows «المتابعة بحساب Google» above the mail form + the admin-only preview note, and tapping it opens Google's own native add-account flow (client config accepted at launch; the sign-in itself is not completable on the emulator — no Google account there, accounts stay the owner's).
  - REMAINING to close M46: (a) owner installs build #3 on his Android phone (expo.dev artifact of build 2f52a234) and taps the Google button — it must be a Google account whose e-mail matches a hub ADMIN contact (preview rule-9 gate; `registrationOpen: false`, so no new accounts either way); if Google errors only AFTER picking the account (code 10 / DEVELOPER_ERROR), suspect the ANDROID client's SHA-1; (b) production Android client id (Play App Signing SHA-1) with the store build (M8); (c) Apple rides the first iOS build (M7): enable «Sign in with Apple» on the App ID — and the owner also asked for an iCloud button on the WEB widget (needs an Apple Services ID, same M7 gate). Site-widget Google button: shipped as M49.

- [ ] **M45 — Pay from inside the site plugin** (owner, 2026-09-23: «لسه هنعدل بلجن نفسها صح؟ بحيث اقدر ادفع من بلجن نفسها»). The hub plugin itself starts Paymob payments (server-side intention carrying the payer's contact identity and an amount from the hub's amounts table) instead of the site's static anonymous Paymob links, so the EXISTING callback — URLs and keys untouched (rule 6) — matches and auto-activates a known payer; no more «دفعة ناجحة بمبلغ غير معروف من NA NA». CODE DONE (2026-09-23, this session; hub plugin 2.8.0, docs/BRIDGE_V2.md 1.7a): public page `/?vai-pay[=key]` (amounts-table cards, name/phone/e-mail, honeypot + IP throttle) → REST `paymob/start` → intention with the payer's billing data, `notification_url` PINNED per payment to the site's own existing callback (so even integration 28390 — whose dashboard callback points at Railway — lands on the site) and `redirection_url` → `/?vai-pay-done` (display only, rule 5). New hub settings: `paymob_secret` / `paymob_public` / `paymob_methods` — empty = page closed, site unchanged. VERIFIED on the rig (real WP): full clean cycle A 33/33, B 136/138 (the two known backfill-series counts, present on the untouched baseline too — diff-proven not ours), C 46/46, D 51/51, C2 17/17, M32 10/10, M37 9/9, NEW m4546-e2e 22/22 (untrusted refused, find-or-create once, intention carries payer + table amount + pinned URLs, signed callback matched and auto-activated the payer, honeypot/rate/off states), zip installed through Plugin_Upgrader, PHP log clean. Delivered: `vibes-ai-assistant-2.8.0.zip` in New Era (2.7.2 recycled) + «خطوات-التشغيل-2026-09-23-مساء.md»; the owner uploaded 2.8.0 to vcmem and filled the pay keys the same evening (bridge says 2.8.x ok; the page renders live).
  - **2.8.1 (same evening, owner's live feedback)**: (a) the pay page is now a FULL registration — a guest must enter name/phone/e-mail **+ password**; the account is created ONLY by the HMAC-verified callback (رفضه الصريح: «مش أي حد يسجل هنا يتفعله عضوية — لازم نتأكد انه فعلا دفع») with the chosen password (create_account_for gained `$pass_hash`; profile parked as a 48h transient keyed by `special_reference`, hash only; an existing password is NEVER overwritten — no takeover via payment); membership item → instant activation, service item → verified account without membership; (b) a signed-in visitor (widget cookie `vai_uuid` → visitors table, server-side) sees «أهلًا {name}», picks ONLY the service — identity comes from his account, nothing retyped («ميملاش بياناته مرتين»), with an «ادفع ببيانات أخرى» escape; an unknown/loggedout visitor gets the mandatory register-and-pay form. m4546-e2e grew to 33 checks (password really signs in through hub `login`; no account before the callback; account mode billing = the account; guest/member page variants). LIVE amounts-table bug found by the owner: the site's «جدول المبالغ» rows are 10× (displayed 19,000 instead of 1,900) — HE fixes the rows to halalas (ريال×100): `190000|membership`, `250000|pitchdeck`, `1500000|urmeet`, `29000|workshop`; this ALSO makes old-link payments finally match and auto-activate (the M44 «مبلغ غير معروف» root cause).
  - **⛔ BLOCKED (2026-09-23 night, owner: «سجل ان دي واقفة واننا محتاجين نشتغل عليها»)**: the page is LIVE and correct (2.8.2 on vcmem: account-mode greeting, right prices, register form) but STARTING a payment fails — Paymob answers **HTTP 404** on `POST /v1/intention/` with the LIVE keys + integration 28390 (2.8.2's settings diagnostic confirmed at 19:08). Same illness as the old test-keys ticket (33439): the **Intention API is not enabled on the Paymob account**. NOT a key-paste error, NOT our code — the identical request through Railway (M44 paylinks/agenda) is expected to 404 too (the 5-SAR test never ran). WAITING ON: the owner's support ticket to Paymob (message drafted in chat 2026-09-23 ~19:30, bilingual, quotes the 404 + both integrations) — once Paymob enables it, the page works with ZERO code changes, then the campaign/site/links-library all move to `/?vai-pay=membership`. UPDATE (2026-09-23 night): Paymob support's FIRST reply was only a canned link to their WordPress (WooCommerce) plugin docs — not applicable (we are server-to-server, and the Woo checkout would bypass the hub's identity/callback design). Claude's keyless probe: `POST https://ksa.paymob.com/v1/intention/` with a dummy token answers **401 «Authentication credentials were not provided»** — the endpoint EXISTS and is routed, so the 404 with the real keys is an ACCOUNT-level refusal (feature not enabled for this merchant), exactly the original diagnosis; our base URLs were already correct (`ksa.paymob.com` in server config.ts and plugin PAYMOB_BASE). A firm bilingual follow-up for the ticket was handed to the owner in chat (insist on enabling Intention/Unified Checkout, don't touch keys/integrations/callbacks).
  - CAMPAIGN INTERIM (running ads tonight): a NEW Paymob dashboard payment link (1,900 SAR) with **required customer info** (name/phone/e-mail) replaces the old anonymous link in the ads — payments then arrive with real identity and the 2.8.1 callback auto-creates/activates (proven live: the 19:13 NA-NA payment auto-created+activated, just on junk identity because the OLD link sent NA NA; that payer turned out to be a REAL registered customer عبدالله 0551921303 who then sent a proof screenshot — owner activates HIM manually one last time and deletes the NA NA account). TODO 2.8.3: refuse auto-account-creation for junk billing identities (na@/no phone) — record + «فعّل يدويًا» mail, no password link to garbage addresses. → SHIPPED inside 2.10.0 (M47 session): both-channels-junk → recorded + «فعّل يدويًا» mail, NO account, rematch skips it, no receipt/ticket mail to junk addresses; junk e-mail + real phone → account by phone only (the junk address is never stored on or matched to a contact — it stays on the payment row for review).

- [ ] **M47 — In-site payment gate, registration-first** (owner, 2026-09-23 evening, after trying the site's OLD static link: «جربت ادفع وداني paymob عادي جدا… هيعرف ازاي العضو انه مينفعش يدفع مباشر وانه لازم يسجل عضوية الأول؟ وبوابة الدفع مظهرتش جوا بلجن ولا جوا السايت نفسه بوب اب مثلا او ظهر الكارت بحيث اني ادفع جوا السايت نفسه، حتي لو ده محتاج تعديل في الويب سايت نفسه… ابعتلك الكود نعدله. المهم اي عضو يسجل يكون عندي بياناته كاملة وبمجرد ما يدفع يتفعل عضويته مباشر بدون تدخل يدوي»). Scope on top of M45: (1) the site's pay buttons open a MODAL inside the page (no visible leave-the-site jump); (2) registration-FIRST gate — a visitor who is not a signed-in hub contact is sent through the widget registration (full data: category, bio…) before he can pay; a signed-in contact pays with his identity attached automatically (no retyping); (3) card entry itself stays on/inside Paymob's secure surface (hosted checkout in the modal/new window, or Paymob's embed SDK if it allows framing — to be tested; we never touch card data, rule 5 spirit); (4) the owner will hand the site page's code to be edited together. UPDATE same evening: points (2) registration-first and no-retype-for-members shipped early inside plugin 2.8.1 (see M45); WHAT REMAINS for M47 is only the in-site MODAL experience on the site's own pages — the checkout itself stays on Paymob's secure surface. CODE DONE (2026-09-23/24 night, this session; hub plugin **2.10.0** in New Era, 2.9.0 recycled) — built INSIDE the plugin, no page code needed: widget.js intercepts clicks on SAME-origin `?vai-pay` links (satellites keep normal navigation — the vai_uuid cookie lives on the hub) and opens the pay page in a full-screen transparent iframe; the page's new `vai-embed` mode turns itself into a translucent overlay with a ✕ (postMessage `vai-pay-close`, ESC and backdrop close too), and the checkout redirect breaks out to the TOP window (`window.top.location`) so card entry NEVER happens in-frame — rule 5 spirit kept. `window.VAIPay.open/close` exposed for future site buttons; a `vai-pay-done` link is never intercepted. PLUS the M45 «2.8.3» junk-identity guard (see there). Verified: full clean isolated cycle green (A 33/33, B 136/138 the known pair, C 46/46, D 51/51, C2 17/17, M32 10/10, M37 9/9, m4546 33/33, m49 20/20, NEW m47-e2e 27/27), zip installed through Plugin_Upgrader → ping 2.10.0, and the modal SEEN working in a real browser on the rig (open over the page, card + ✕, close chain). NOTE: the modal is dormant until `?vai-pay` links return to the site (M50 pulled them for now) — zero effect on the live campaign. UPDATE (same night): the owner uploaded 2.10.0 himself and sent a live screenshot of the MODAL rendering on vcmem (a visitor filling the form — payment start still 404, which became M50 part 2); superseded by **2.10.1** in New Era (adds the M50 direct-interim mode; same M47 code otherwise).

- [ ] **M50 — Emergency: back to the DIRECT pay link + «no two-year offer ever» (n8n v4.4)** (owner, 2026-09-23 night: «خد بالك مفيش اشتراك سنتين ده خالص. ولو في بلاش تبعت للناس دلوقتي خالص الرابط اللي مش شغال ابعت ليهم الرابط بتاع paymob مباشر لان بلجن عملت مشاكل مع العملاء»): the vai-pay page cannot START payments while Paymob's Intention API stays disabled (M45 ticket), and v4.3 was steering customers there. CODE DONE (this session; New Era «3- عقل n8n v4.4», v4.3 recycled): prompt-v4.md — web payment = the DIRECT Paymob link from catalog/links-library, the automatic page declared «خارج الخدمة مؤقتًا» (never sent, even from old turns), the no-receipts rule kept, and a NEW hard rule «العضوية سنوية فقط — لا يوجد أي عرض سنتين أو سعر ترويجي آخر إطلاقًا» (build FAILS if either line goes missing); build-v4.mjs — `?vai-pay` links are scrubbed from the links-library context AND from the reply text/buttons on WEB and app alike (guard `vai-pay-out:n`), the sign-up welcome pitches the direct link, self-tests grew (context filter, reply+button scrub with CTA words swallowed, prompt rules). Built from the untouched v3.2 export: Config byte-identical, self-test ok, prompt 10,195 chars. OWNER (خطوات-التشغيل-2026-09-24.md, URGENT FIRST): put the NEW required-info Paymob link (the campaign one) in the catalog pay_url + links-library, delete any «سنتين/سعر ترويجي» line from التوجيهات, import v4.4, deactivate v4.3, test «كيف أشترك؟». When Paymob enables Intention: v4.5 flips the automatic page back on (one line) + catalog/links move to `?vai-pay=membership`.
  - **Part 2 — the page's own button goes DIRECT (2.10.1)** (owner, same night, with a live screenshot of the vai-pay MODAL on vcmem failing at start — which also proved he had uploaded 2.10.0 himself, `widget.js?ver=2.10.0` verified live: «خلي الزرار اللي تحت ده يحول بشكل مباشر علي الدفع مفيش حل غير كده ونشوف اي طريقة نتراك بيها العملية»). CODE DONE (this session; hub **2.10.1** in New Era, 2.10.0 recycled): new setting «رابط الدفع المباشر (مؤقت)» — while filled it WINS over the intention keys: the pay button hands the visitor straight to the direct Paymob link (no more gateway error), and the page tells him to type THE SAME phone/e-mail there. TRACKING: the typed register-and-pay profile (name/phone/e-mail + password HASH, 48h transients keyed by identity — a dashboard-link callback carries no reference of ours) is applied by the HMAC-verified callback: both-channels match + account CREATED by that callback → his chosen password works at once; half-match → normal auto-create, password NOT applied (never on an existing account either — no takeover path); every joined payment's admin mail gains «بدأ الطلب من صفحة الدفع بالموقع». Emptying the setting restores full automatic mode with zero re-upload. Verified: full clean cycle green again with m47-e2e grown to **37/37** (direct start returns the link itself, no account before the callback, password really signs in, half-match activates without the password, account mode direct, direct wins over saved intention keys), zip via Plugin_Upgrader → ping 2.10.1. Remaining: owner uploads + pastes links + imports v4.4 if not yet done. **Part 3 — one link PER SERVICE (2.10.2)** (owner, minutes later, with a screenshot of the 2.10.1 field he had ALREADY uploaded: «في اكتر من رابط دفع يعني مش رابط واحد دي خدمات كتير اضيف اي واحد فيهم؟» — right: one link would send a 290-workshop buyer to the 1,900 link): `paymob_direct` became a textarea, one line per service `key | https://…` (keys from the amounts table) + an optional keyless line as the default; in direct mode a service WITHOUT a link leaves the page and its start is refused (a fixed-amount link can never be reached from the wrong item); a plain single-URL value still works as the default for all (2.10.1 compatibility). Cycle green a third time, m47-e2e **40/40** (linked-only offering, per-service URLs, unlinked start 400, keyless default restores everything), zip via Plugin_Upgrader → ping 2.10.2; New Era «1- بلجن المساعد 2.10.2» (2.10.1 recycled), steps file shows the exact paste block. Remaining: owner uploads and pastes links. **Part 4 — the admin save GLUED the lines (2.10.3)** (owner, minutes later, live: pasted-and-saved 3 links yet the page still errored with ALL services showing): 2.10.2 forgot to add `paymob_direct` to the textarea-sanitize list, so `sanitize_text_field` collapsed the 3 lines into one and the strict line parser read NOTHING (the rig never caught it — tests set the option via `vai_update_settings`, bypassing the admin form). FIXED in **2.10.3** (New Era, 2.10.2 recycled): the field saves as a textarea, AND `pay_direct_map` now regex-extracts every `key | https://…` pair wherever it stands, so even an already-glued saved value reads fully. Cycle green a fourth time, m47-e2e **41/41** (glued-line check), zip via Plugin_Upgrader → ping 2.10.3. LIVE INTERIM applied by Claude in the pane (owner's «ظبط علي دول» + the links in chat): a SINGLE line `membership | https://paymob.link/VIRWW` saved on live 2.10.2 (one line survives the glue bug) — verified live: the pay page now offers ONLY membership with the direct note, gateway error GONE; his real links: workshop `https://paymob.link/tH3EE`, urmeet `https://paymob.link/vkfVr` (no pitchdeck link) — they return once 2.10.3 is uploaded and the 3 lines are pasted (steps file carries the copy-ready block). LESSON for the rig: admin-form saves are NOT covered by `vai_update_settings`-based tests — a new textarea setting must be added to the sanitize list at class-vai-admin.php ~line 336 AND smoke-tested through the real form.
- [ ] **M51 — Strict profile completion after Google sign-up** (owner, 2026-09-23 23:41, with the admin-notify mail of the FIRST real Google-button member — «Hani», name+e-mail only, جوال/الوظيفة/الفئة/نبذة all empty: «وعايزك تشدد اوي اوي اوي علي اني المعلومات دي تكتب بشكل صحيح»). Why it happens: Google gives only the name and the verified e-mail — the social path (M46/M49) creates the account without the registration form, so phone/category/bio stay empty. Scope (proposed, pending the owner's go): after a social sign-up the widget opens a REQUIRED complete-your-profile step (جوال أولًا — قناة النادي الأساسية — ثم الفئة والنبذة) before he continues; the app does the same after its Google/Apple sign-in; the n8n welcome nudges for the missing جوال; and the notify mail prints «—» for an empty phone like the other fields (cosmetic). LIVE STATUS of the night otherwise: 2.10.3 uploaded by the owner, Claude pasted the 3 direct links in the pane and the pay page offers الع​ضوية/الورشة/اصنع ملتقاك live (pitchdeck hidden, no link). (owner, 2026-09-23 night, while live-testing 2.8.1: «اعتقد في تعديلات كتير جدا حصلت محتاجين نعدل العقل الاساسي n8n بحيث ياخد مننا التعليمات الجديدة ويفهم العملاء بالظبط يعملوا ايه»). Teach the assistant (Vibes Web Agent v4.2) the new reality: `/?vai-pay` IS the way to pay/subscribe on the web (register-and-pay with password, account only after CONFIRMED payment, instant activation; a signed-in member pays with nothing retyped) — the assistant sends people THERE instead of the old static Paymob links; mention Google/Apple sign-in in the app. Method as before: regenerate from the owner's export with the build script (Config node untouched, output to his drive, never git). Also update the hub links-library line «الاشتراك المباشر في العضوية (Paymob)» → `https://vcmem.com/?vai-pay=membership` (the owner can do that NOW from the settings' links textarea). Live case 19:18 that sharpened it (owner: «بجد حوار انه مش اوتوماتيك ده عاملي مشكلة»): a REAL registered customer (عبدالله، 0551921303) paid through the OLD link → billing NA NA → no auto-match, then sent the assistant a payment-proof screenshot → «تفعيل قيد المراجعة». The new brain must send people to the automatic payment (never collect proof screenshots as the primary path; proof mail stays a fallback only) — and rule 5 stands: a screenshot NEVER auto-activates, only the HMAC callback does. DONE (2026-09-23, this session): **v4.3** built from the untouched v3.2 export → New Era «3- عقل n8n v4.3» (`Vibes_Web_Agent_v4.3.json`, Config byte-identical, self-test ok, prompt 10,013 chars). prompt-v4.md: web payment = the automatic pay page ONLY — register-and-pay, instant auto-activation after the CONFIRMED payment, a signed-in member retypes nothing, links copied from catalog/links-library only; NEVER ask a would-be payer for a proof screenshot (proof stays a fallback for someone who already paid; rule 5 restated — activation only from the confirmed payment); new sign-in facts line (Google button in the account sheet where it shows, Google/Apple in the app, one account everywhere). build-v4.mjs: NAME v4.3, the app-mode scrub also blocks `vai-pay` URLs (context + reply + admin-notes PAY_TALK — closes the rule-3 leak the new link would have opened), the sign-up welcome handles `social: google` (hub 2.9.0 sends it) and points at the automatic pay page, self-tests grew (vai-pay kept on web, scrubbed in app; Google welcome). OWNER (in the steps file, BEFORE importing): catalog pay_url + links-library line → `https://vcmem.com/?vai-pay=membership`, then import v4.3 and deactivate v4.2 (same webhook path). UPDATE (same night, LIVE): the owner imported v4.3 into n8n himself; a live anonymous widget question «كيف أشترك في العضوية؟» answered in ~40 s with the new logic (electronic pay, «واصل بحساب Google», instant activation, NO proof-screenshot ask). Catalog/links-library still carry the old Paymob URLs on purpose — they switch to `?vai-pay=membership` only when Paymob enables the Intention API (M45 ticket). ⚠ The reply offered «سنتين بالسعر الترويجي نفسه» — RESOLVED in M50 (owner 2026-09-23 night: «مفيش اشتراك سنتين ده خالص»): v4.4 carries a hard annual-only rule and the steps file tells the owner to delete the stale «سنتين» line from التوجيهات.

- [x] **M49 — Google button in the SITE widget** (owner, 2026-09-23 night, showing the widget's «حساب جديد» form: «تسجيل جوجل لسه مشتغلش علي فكره» — he expects the web widget to offer it too, as in his first M46 ask). Scope: «المتابعة بحساب Google» inside the widget's register/login step — Google Identity Services JS → ID token → NEW hub-side verification in PHP (JWKS RS256, audience = the SAME web client id `980470249678-…`), then the same find-or-create-verified path as `social_login` (shared helper; the trusted-op stays app-server-only, the widget path verifies ON the hub itself); Google Cloud: add each site origin to the web client's «Authorized JavaScript origins» (start with vcmem.com, the rest of the 13 sites after); widget.js UI + the `/client` proxy for satellites later. Apple web (Services ID) stays out until Apple console exists. Distinct from the APP button (M46 — riding the queued preview build). DONE (2026-09-23, this session; hub plugin **2.9.0** in New Era, 2.8.2 recycled): widget register/login sheets gained «المتابعة بحساب Google» (GIS renderButton; shows only when the NEW hub setting `google_client_id` is filled — empty = nothing changes anywhere); NEW public site op `google`: the hub verifies the ID token ITSELF (`includes/class-vai-google.php`: JWKS cached 6 h + injectable via filter `vai_google_jwks`, RS256 through OpenSSL over a hand-built SPKI, iss/aud/exp/email_verified checks) → shared `social_account()` extracted from `social_login` (which stays trusted-only): find-or-create VERIFIED, an existing password is NEVER touched, and BOTH social paths now also run op_verify's pre-paid member_lookup auto-activation; a non-trusted (widget) site gets the n8n `identified` welcome (`social: google` on creation, fallback line when n8n is down); `/client` proxy carries `google`, so every satellite rides once its origin is added to the SAME web client id. Rig: full clean cycle green (A 33/33, B 136/138 the known pair, C 46/46, D 51/51, C2 17/17, M32 10/10, M37 9/9, m4546 33/33) + NEW `m49-e2e` 20/20 (forged/expired/wrong-audience/unverified-e-mail/alg-none all refused 401; off-switch refuses and hides the button; no duplicate account; password intact; hub + satellite /client paths; JWKS injected through the rig's mu-plugin), zip installed through Plugin_Upgrader, ping 2.9.0, PHP log clean. OWNER steps (خطوات-التشغيل-2026-09-23-ليلًا.md): add site origins in Google Cloud (vcmem first, ideally all 13 at once), upload 2.9.0, paste the web client id in the new settings section; satellites: origin BEFORE their plugin upgrade. UPDATE (same night, LIVE, Claude driving the pane): origins `https://vcmem.com` + `https://www.vcmem.com` added and SAVED on the web client (verified after a reload; the Cloud-console virtual viewport shows an empty form right after Save — reload before believing it failed), consent screen was already In production; the owner uploaded 2.9.0 (plugins page reads 2.9.0; the auto-mode classifier refuses ANY live-deploy action from Claude, even opening plugin-install.php — uploads are the owner's clicks); Claude filled `google_client_id` in the settings and saved («تم حفظ الإعدادات», value verified after reload); the branded GIS button renders LIVE in the widget's register/login sheets (label follows the browser/Google-account language — English on Claude's pane, Arabic for Arabic visitors). Left: the final click-through sign-in (the pane blocks popups born from Claude's synthetic clicks — one owner click), the remaining sites' origins BEFORE their 2.9.0 upgrade, and Apple web (needs the Apple Developer account — rides M7; the owner asked tonight for an iCloud button too).

- [ ] **M8 — Store readiness and launch**: privacy policy, account deletion web page, data safety forms, demo account for Apple review, launch. The data-safety forms and privacy policy must declare the M33 usage analytics: the app sends opened screen names with a random device id to our own server (first-party analytics, aggregates per day, no third-party SDK, no browsing history per member).

**Owner-console gaps, consolidated (2026-09-23 — none of them code; Claude hands the steps, the owner clicks):**
1. ~~Push delivery~~ RESOLVED in M42 (leaked key deleted, fresh FCM V1 key on expo.dev, real pushes delivered). Note: `/health` `push.tokens/sent` are since-boot counters — they read 0 right after every Railway deploy until the first register/send; the tokens themselves persist in postgres.
2. M18 dashboard e-mail code: SMTP is configured; set `ADMIN_OTP=1` on Railway, sign in once and time the mail — if it takes longer than a minute set `ADMIN_OTP_SECONDS=180`. (`/health` → `hub.adminOtp`, still `off` 2026-09-23.)
3. Plugin uploads (New Era): hub is now 2.9.0 LIVE on vcmem.com ✅ (M32/M45/M49 all active). Still pending: Projects Bank 39.2 zip on vibesholding.com (M37 gate + the instant `project.changed` webhook; after it, dashboard home «حالة الربط» should say pb 39.2 ok).
4. Railway: Hobby plan PAID ✅ (M42). Still pending: rename service `thorough-cooperation` → `admin`.
5. M7 remainder (stores): Play Console + App Store Connect subscription `club_membership_annual`, RevenueCat keys on Railway, then ONE preview build for purchases; the iOS build after.
